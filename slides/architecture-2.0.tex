\documentclass[aspectratio=169,10pt]{beamer}

\usepackage[T1]{fontenc}
\usepackage{sourcesanspro}
\usepackage{sourcecodepro}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{fontawesome5}
\usetikzlibrary{arrows.meta,positioning,fit,calc,shapes.geometric}
\pgfplotsset{compat=1.18}

\definecolor{ArchInk}{HTML}{20252B}
\definecolor{ArchMuted}{HTML}{59636D}
\definecolor{ArchBlue}{HTML}{1F5F7A}
\definecolor{ArchBlueLight}{HTML}{EAF3F7}
\definecolor{ArchGreen}{HTML}{3D7A57}
\definecolor{ArchGreenLight}{HTML}{EDF6EF}
\definecolor{ArchRed}{HTML}{A42A38}
\definecolor{ArchRedLight}{HTML}{FAECEE}
\definecolor{ArchGold}{HTML}{B47A18}
\definecolor{ArchGoldLight}{HTML}{FBF3E3}
\definecolor{ArchLine}{HTML}{C8D1D8}
\definecolor{ArchPanel}{HTML}{F6F8F9}

\setbeamercolor{normal text}{fg=ArchInk,bg=white}
\setbeamercolor{structure}{fg=ArchBlue}
\setbeamercolor{frametitle}{fg=ArchInk,bg=white}
\setbeamercolor{alerted text}{fg=ArchRed}
\setbeamerfont{title}{size=\fontsize{26}{30}\selectfont,series=\bfseries}
\setbeamerfont{subtitle}{size=\fontsize{13}{16}\selectfont}
\setbeamerfont{frametitle}{size=\fontsize{18}{22}\selectfont,series=\bfseries}
\setbeamerfont{footline}{size=\fontsize{6.8}{8}\selectfont}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{itemize item}{\color{ArchBlue}\small$\blacksquare$}
\setbeamertemplate{itemize subitem}{\color{ArchMuted}\scriptsize$\blacktriangleright$}
\setbeamersize{text margin left=0.55cm,text margin right=0.55cm}

\setbeamertemplate{frametitle}{%
  \nointerlineskip
  \begin{beamercolorbox}[wd=\paperwidth,ht=0.72cm,dp=0.13cm,leftskip=0.58cm,rightskip=0.58cm]{frametitle}
    \usebeamerfont{frametitle}\insertframetitle
  \end{beamercolorbox}
  \vspace{-0.08cm}
  \hspace{0.58cm}\color{ArchBlue}\rule{1.15cm}{0.75pt}\color{ArchLine}\rule{\dimexpr\paperwidth-2.31cm\relax}{0.35pt}
  \vspace{0.12cm}
}

\setbeamertemplate{footline}{%
  \leavevmode
  \hbox{%
    \begin{beamercolorbox}[wd=.53\paperwidth,ht=0.31cm,dp=0.12cm,leftskip=0.58cm]{footline}
      \color{ArchMuted}Architecture 2.0 \enspace|\enspace AI-Native System and Chip Design
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.29\paperwidth,ht=0.31cm,dp=0.12cm]{footline}
      \hfill\color{ArchBlue}\insertsectionhead
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.18\paperwidth,ht=0.31cm,dp=0.12cm,rightskip=0.58cm plus 1fil]{footline}
      \hfill\color{ArchMuted}\insertframenumber
    \end{beamercolorbox}%
  }
}

\newcommand{\claim}[1]{%
  \begin{beamercolorbox}[rounded=true,sep=0.12cm,wd=\linewidth]{block body}
    \color{ArchBlue}\bfseries #1
  \end{beamercolorbox}\vspace{0.12cm}
}
\newcommand{\source}[1]{%
  \vfill{\fontsize{5.8}{7}\selectfont\color{ArchMuted}Source: #1\par}
}
\newcommand{\tagbox}[3]{%
  \begin{tikzpicture}[baseline=(n.base)]
    \node[rounded corners=2pt,fill=#1,draw=#2,inner xsep=5pt,inner ysep=3pt,
      text=ArchInk,font=\small\bfseries] (n) {#3};
  \end{tikzpicture}
}
\newcommand{\bigsep}[1]{%
  \begin{center}
    \begin{tikzpicture}
      \node[rounded corners=3pt,fill=ArchBlueLight,draw=ArchBlue,
        text width=13.2cm,align=center,minimum height=1.0cm,
        font=\fontsize{17}{20}\selectfont\bfseries,text=ArchBlue] {#1};
    \end{tikzpicture}
  \end{center}
}

\title{Architecture 2.0}
\subtitle{Principles of AI-Native System and Chip Design}
\author{Vijay Janapa Reddi}
\date{}

\begin{document}

\section{Moonshot}

% 1
\begin{frame}[plain]
  \begin{tikzpicture}[remember picture,overlay]
    \fill[ArchBlue] (current page.north west) rectangle ([xshift=0.14cm]current page.south west);
  \end{tikzpicture}
  \vspace{0.55cm}
  {\color{ArchMuted}\small SYNTHESIS LECTURE\par}
  \vspace{0.35cm}
  {\usebeamerfont{title}\color{ArchInk}\inserttitle\par}
  \vspace{0.12cm}
  {\usebeamerfont{subtitle}\color{ArchMuted}\insertsubtitle\par}
  \vspace{0.45cm}
  \begin{center}
    \includegraphics[width=0.72\linewidth,height=3.35cm,keepaspectratio]{../book/images/arch2-loop-diagram.pdf}
  \end{center}
  \vfill
  {\small\color{ArchInk}\insertauthor\hfill\color{ArchMuted}arch2.mlsysbook.ai\par}
\end{frame}

% 2
\begin{frame}{Three fields, one architecture decision}
  \claim{The useful intersection is not AI applied to chips. It is an architecture result that can support a decision.}
  \begin{columns}[c,onlytextwidth]
    \column{0.58\textwidth}
      \includegraphics[width=\linewidth,height=4.65cm,keepaspectratio]{../book/images/fig-book-composition.pdf}
    \column{0.38\textwidth}
      \vspace{0.45cm}
      \begin{itemize}
        \item Architecture supplies the tradeoff and system boundary.
        \item ML supplies possible prediction, generation, and optimization methods.
        \item EDA supplies executable constraints and physical observations.
      \end{itemize}
  \end{columns}
\end{frame}

% 3
\begin{frame}{The Moonshot: from intent to a supported design}
  \claim{What if an AI-assisted design system could turn a compact architecture intent into an implementable, verified, and reviewable system design?}
  \begin{columns}[T,onlytextwidth]
    \column{0.66\textwidth}
      \includegraphics[width=\linewidth,height=4.55cm,keepaspectratio]{../book/contents/chapters/01-moonshot/images/F1-moonshot-stack.pdf}
    \column{0.30\textwidth}
      \vspace{0.25cm}
      \tagbox{ArchGoldLight}{ArchGold}{A field-scale target}
      \vspace{0.18cm}
      \begin{itemize}
        \item low-power, 64-bit RISC-V compute subsystem
        \item real-time mobile extended-reality (XR) workloads
        \item software, RTL, physical, verification, and product constraints
      \end{itemize}
  \end{columns}
  \source{Architecture 2.0, Chapter 1; XRBench and ILLIXR ground the workload class.}
\end{frame}

% 4
\begin{frame}{Four different success claims}
  \claim{A candidate, a supported architecture result, the contribution of the complete AI-assisted approach, and moonshot capability are distinct claims.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.42cm and 0.44cm]
    \tikzset{claimstep/.style={rounded corners=3pt,draw=ArchLine,fill=ArchPanel,
      text width=2.72cm,minimum height=1.35cm,align=center,font=\small}}
    \node[claimstep] (a) {\textbf{Candidate or artifact}\\A legal artifact exists};
    \node[claimstep,right=of a] (b) {\textbf{Supported architecture result}\\The comparison supports a technical recommendation};
    \node[claimstep,right=of b] (c) {\textbf{Complete-approach contribution}\\A matched comparison tests whether the AI-assisted approach helped};
    \node[claimstep,right=of c] (d) {\textbf{Moonshot capability}\\Repeated matched comparisons cover declared tasks and conditions};
    \node[below=0.42cm of b.south east,align=center,text=ArchRed,font=\small\bfseries]
      {None of these claims authorizes architecture commitment.};
  \end{tikzpicture}
  \end{center}
\end{frame}

% 5
\begin{frame}{The Architecture 2.0 bet}
  \bigsep{Improve the architecture result, reduce the total cost of reaching a comparable result, or both.}
  \vspace{0.35cm}
  \begin{columns}[T,onlytextwidth]
    \column{0.31\textwidth}
      \tagbox{ArchBlueLight}{ArchBlue}{Architecture quality}
      \begin{itemize}
        \item performance, power, area
        \item reliability, security
        \item software and product fit
      \end{itemize}
    \column{0.31\textwidth}
      \tagbox{ArchGreenLight}{ArchGreen}{Total cost}
      \begin{itemize}
        \item model and tool calls
        \item queue and license time
        \item human setup, review, repair
      \end{itemize}
    \column{0.31\textwidth}
      \tagbox{ArchRedLight}{ArchRed}{Failure behavior}
      \begin{itemize}
        \item invalid candidates
        \item missed constraints
        \item unsafe or uncontrolled actions
      \end{itemize}
  \end{columns}
\end{frame}

% 6
\section{Why assistance}

\begin{frame}{Every efficiency lever adds obligations}
  \claim{Scaling gave way to specialization, composition, and cross-layer work.}
  \begin{center}
    \includegraphics[width=0.84\linewidth,height=5.0cm,keepaspectratio]{../book/contents/chapters/02-design-loop/images/F2-architecture-levers.pdf}
  \end{center}
  \source{Dennard et al. (1974); Amdahl (1967); Esmaeilzadeh et al. (2011); UCIe Consortium (2026); Barroso, Hölzle, and Ranganathan (2019). Synthesis in Chapter 2.}
\end{frame}

% 7
\begin{frame}{Data movement can dwarf arithmetic}
  \claim{The system boundary decides whether a local compute gain matters.}
  \begin{center}
  \begin{tikzpicture}
    \begin{axis}[
      width=12.6cm,height=5.1cm,
      xbar,xmode=log,
      xmin=0.06,xmax=4000,
      symbolic y coords={32-bit integer add,8 KB SRAM access,1 MB SRAM access,64-bit DRAM access},
      ytick=data,
      y dir=reverse,
      xlabel={rough energy per operation or access (pJ, log scale)},
      axis x line*=bottom,axis y line*=left,
      tick label style={font=\small},
      label style={font=\small},
      point meta=explicit symbolic,
      nodes near coords={\pgfplotspointmeta},
      nodes near coords align={horizontal},
      every node near coord/.append style={font=\small,anchor=west},
      bar width=10pt,
      grid=major,
      grid style={ArchLine!45},
    ]
      \addplot[fill=ArchBlue,draw=ArchBlue] coordinates
        {(0.1,32-bit integer add) [0.1]
         (10,8 KB SRAM access) [10]
         (100,1 MB SRAM access) [100]
         (1950,64-bit DRAM access) [1950]};
    \end{axis}
  \end{tikzpicture}
  \end{center}
  \source{Horowitz, ISSCC 2014 rough 45 nm energy anchors. DRAM is plotted at the midpoint of the reported 1300--2600 pJ range; these are order-of-magnitude anchors, not current-node predictions.}
\end{frame}

% 8
\begin{frame}{Cost has moved beyond drawing the RTL}
  \claim{At advanced nodes, software, verification, and IP qualification can dominate estimated design cost.}
  \begin{columns}[T,onlytextwidth]
    \column{0.60\textwidth}
      \begin{tikzpicture}
      \begin{axis}[
        width=8.2cm,height=4.0cm,
        ybar stacked,
        ymin=0,ymax=800,
        symbolic x coords={7 nm,2 nm},xtick=data,
        ylabel={estimated design cost (\$M)},
        bar width=28pt,
        tick label style={font=\small},
        label style={font=\small},
        legend style={font=\scriptsize,draw=none,at={(0.5,-0.18)},anchor=north,legend columns=1},
        axis x line*=bottom,axis y line*=left,
        grid=major,grid style={ArchLine!45},
      ]
        \addplot[fill=ArchBlue,draw=ArchBlue] coordinates {(7 nm,0) (2 nm,514.75)};
        \addplot[fill=ArchLine,draw=ArchMuted] coordinates {(7 nm,249) (2 nm,210.25)};
        \legend{software + verification + IP qualification,other or composition not stated}
      \end{axis}
      \end{tikzpicture}
    \column{0.35\textwidth}
      {\fontsize{34}{38}\selectfont\bfseries\color{ArchBlue}71\%\par}
      \vspace{0.1cm}
      \textbf{of the \$725M 2 nm estimate}\\
      was attributed to the combined software, verification, and IP-qualification categories.
      \vspace{0.4cm}

      \color{ArchMuted}\small The 7 nm composition was not published, so the chart does not invent it.
  \end{columns}
  \source{Arm Holdings, Form 424B4 (2023), reporting July 2022 IBS estimates: \$249M at 7 nm and \$725M at 2 nm.}
\end{frame}

% 9
\begin{frame}{Production can outpace examination}
  \claim{Faster candidate creation is useful only if evaluation and review can keep up.}
  \begin{center}
    \includegraphics[width=0.88\linewidth,height=4.65cm,keepaspectratio]{../book/contents/chapters/02-design-loop/images/F2-scissors-gap.pdf}
  \end{center}
  \begin{center}\small This is one possible limiting pattern; the next slide diagnoses other cases.\end{center}
\end{frame}

% 10
\begin{frame}{Diagnose the limiting work first}
  \claim{Different delays call for different interventions.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.16cm]
    \tikzset{lane/.style={rounded corners=2pt,draw=ArchLine,minimum width=13.4cm,
      minimum height=0.68cm,align=left,font=\small,inner xsep=8pt}}
    \node[lane,fill=ArchPanel] (a) {\textbf{Missing information}\hspace{0.5cm} retrieve, reconcile, or collect the state};
    \node[lane,fill=ArchPanel,below=of a] (b) {\textbf{Invalid or unhelpful choices}\hspace{0.5cm} constrain or change the candidate space};
    \node[lane,fill=ArchPanel,below=of b] (c) {\textbf{Expensive evaluation}\hspace{0.5cm} predict, screen, or allocate stronger checks};
    \node[lane,fill=ArchPanel,below=of c] (d) {\textbf{Overloaded review}\hspace{0.5cm} change capacity, routing, or review preparation};
    \node[lane,fill=ArchPanel,below=of d] (e) {\textbf{Unclear decision}\hspace{0.5cm} name the action, owner, and evidence threshold};
  \end{tikzpicture}
  \end{center}
  \vspace{0.15cm}
  \begin{center}\color{ArchRed}\bfseries ``Use AI'' is not a diagnosis.\end{center}
\end{frame}

% 11
\begin{frame}{A foundation model supports a larger design system}
  \claim{A foundation model can supply reusable architectural knowledge and capabilities. The complete design system still orchestrates people, methods, tools, project state, and checks.}
  \begin{center}
    \includegraphics[width=0.90\linewidth,height=4.45cm,keepaspectratio]{../book/images/arch2-loop-diagram.pdf}
  \end{center}
\end{frame}

% 12
\section{Lifecycle}

\begin{frame}{System, study, and loop are different}
  \claim{The loop is a possible behavior inside a study, not the whole thesis.}
  \begin{columns}[T,onlytextwidth]
    \column{0.31\textwidth}
      \tagbox{ArchBlueLight}{ArchBlue}{Complete system}
      \begin{itemize}
        \item persistent people, methods, tools, state, checks
        \item supports many studies
      \end{itemize}
    \column{0.31\textwidth}
      \tagbox{ArchGreenLight}{ArchGreen}{Architecture study}
      \begin{itemize}
        \item one question and scope
        \item one decision or honest stop
      \end{itemize}
    \column{0.31\textwidth}
      \tagbox{ArchGoldLight}{ArchGold}{Optional loop}
      \begin{itemize}
        \item another permitted action
        \item only when the result supports it
      \end{itemize}
  \end{columns}
\end{frame}

% 13
\begin{frame}{An architecture study connects question to decision}
  \claim{The study preserves the question, comparison, execution conditions, checks, and decision across tools.}
  \begin{center}
    \includegraphics[width=0.88\linewidth,height=4.4cm,keepaspectratio]{../book/contents/chapters/03-lifecycle/images/F4d-structured-layer-above-rtl.pdf}
  \end{center}
\end{frame}

% 14
\begin{frame}{Six distinct kinds of work}
  \claim{The stages are separated because they answer different questions and fail differently.}
  \begin{center}
    \includegraphics[width=0.94\linewidth,height=5.2cm,keepaspectratio]{../book/contents/chapters/03-lifecycle/images/ch3-ontology-loop.pdf}
  \end{center}
\end{frame}

% 15
\begin{frame}{Repair the part that failed}
  \begin{center}
  \begin{tikzpicture}[node distance=0.28cm and 0.42cm]
    \tikzset{r/.style={rounded corners=2pt,draw=ArchLine,fill=ArchPanel,
      minimum width=4.1cm,minimum height=0.82cm,align=center,font=\small}}
    \node[r] (a) {\textbf{Bad question}\\reformulate};
    \node[r,right=of a] (b) {\textbf{Weak comparison}\\change alternatives or budget};
    \node[r,right=of b] (c) {\textbf{Broken execution}\\repair the environment};
    \node[r,below=of a] (d) {\textbf{Inadequate check}\\strengthen measurement};
    \node[r,right=of d] (e) {\textbf{Failed explanation}\\narrow or retest mechanism};
    \node[r,right=of e] (f) {\textbf{Unresolved decision}\\defer, reject, or assign work};
  \end{tikzpicture}
  \end{center}
  \vspace{0.3cm}
  \begin{center}\color{ArchBlue}\bfseries Iteration should have a technical reason and a destination.\end{center}
\end{frame}

% 16
\begin{frame}{One record holds the comparison together}
  \claim{Another architect should be able to reconstruct what changed, what ran, what failed, and what the result supports.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.18cm and 0.28cm]
    \tikzset{record/.style={rounded corners=2pt,draw=ArchLine,fill=ArchPanel,
      text width=3.75cm,minimum height=0.78cm,align=center,font=\small}}
    \node[record] (a) {\textbf{Question + permitted}\\\textbf{changes}};
    \node[record,right=of a] (b) {\textbf{Comparator + budget}};
    \node[record,right=of b] (c) {\textbf{Environment + tool conditions}};
    \node[record,below=of a] (d) {\textbf{Accepted and rejected}\\\textbf{candidates}};
    \node[record,right=of d] (e) {\textbf{Measurements, failures,}\\\textbf{and limits}};
    \node[record,right=of e] (f) {\textbf{Supported result + accountable action}};
  \end{tikzpicture}
  \end{center}
  \vspace{0.25cm}
  \begin{center}\color{ArchBlue}\bfseries The record connects the technical argument; it is not the argument itself.\end{center}
\end{frame}

% 17
\section{Data and representation}

\begin{frame}{The project knows three different things}
  \begin{center}
  \begin{tikzpicture}[node distance=0.55cm]
    \tikzset{k/.style={rounded corners=3pt,minimum width=12.8cm,minimum height=1.05cm,
      align=left,inner xsep=9pt,font=\small}}
    \node[k,fill=ArchBlueLight,draw=ArchBlue] (a)
      {\textbf{General knowledge}\hspace{0.4cm} mechanisms, specifications, papers, slow-moving relationships};
    \node[k,fill=ArchGreenLight,draw=ArchGreen,below=of a] (b)
      {\textbf{Current project state}\hspace{0.4cm} exact design, workload, software, constraints, owners};
    \node[k,fill=ArchGoldLight,draw=ArchGold,below=of b] (c)
      {\textbf{Project history}\hspace{0.4cm} runs, failures, rejections, adoption, changed conditions};
  \end{tikzpicture}
  \end{center}
  \vspace{0.2cm}
  \begin{center}\color{ArchRed}\bfseries A model's general knowledge is not live project state.\end{center}
\end{frame}

% 18
\begin{frame}{Observation sources are not interchangeable}
  \claim{Choose the source by the property the study must observe, not by a single fidelity ranking.}
  \begin{center}
    \includegraphics[width=0.88\linewidth,height=5.0cm,keepaspectratio]{../book/contents/chapters/04-representations/images/F4-data-pyramid.pdf}
  \end{center}
\end{frame}

% 19
\begin{frame}{An architecture sample can be expensive}
  \claim{Data acquisition is part of the architecture method, not background plumbing.}
  \begin{center}
  \begin{tikzpicture}
    \node[rounded corners=4pt,fill=ArchBlueLight,draw=ArchBlue,
      minimum width=4.1cm,minimum height=2.0cm,align=center] (a)
      {{\fontsize{30}{34}\selectfont\bfseries\color{ArchBlue}994}\\legal microarchitectures};
    \node[rounded corners=4pt,fill=ArchGoldLight,draw=ArchGold,
      minimum width=4.1cm,minimum height=2.0cm,align=center,right=1.1cm of a] (b)
      {{\fontsize{30}{34}\selectfont\bfseries\color{ArchGold}6--14+ h}\\per VLSI-flow evaluation};
    \node[rounded corners=4pt,fill=ArchRedLight,draw=ArchRed,
      minimum width=4.1cm,minimum height=2.0cm,align=center,right=1.1cm of b] (c)
      {{\fontsize{25}{29}\selectfont\bfseries\color{ArchRed}$\geq$5,964 h}\\aggregate evaluation time\\at the reported low end};
    \draw[-{Stealth},ArchMuted,thick] (a)--(b);
    \draw[-{Stealth},ArchMuted,thick] (b)--(c);
  \end{tikzpicture}
  \end{center}
  \vspace{0.3cm}
  \begin{center}\small Ten analytical calls, ten cycle simulations, and ten physical-design runs are not ten equivalent samples.\end{center}
  \source{BOOM-Explorer: Bai et al., ICCAD 2021. The final value is 994 $\times$ 6 h: a serial aggregate, not elapsed wall time.}
\end{frame}

% 20
\begin{frame}{Representation determines reachable designs}
  \claim{A legal proposal can still be invalid when project state and tool-facing artifacts disagree.}
  \begin{center}
    \includegraphics[width=0.87\linewidth,height=4.75cm,keepaspectratio]{../book/contents/chapters/04-representations/images/F4b-split-brain-causality.pdf}
  \end{center}
\end{frame}

% 21
\begin{frame}{Architecture data must become usable state}
  \claim{Data is made through tools and design work; representation determines what a model can learn and what the design system can act on.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.33cm]
    \tikzset{datastep/.style={rounded corners=3pt,text width=2.35cm,
      minimum height=1.35cm,align=center,font=\small}}
    \node[datastep,fill=ArchPanel,draw=ArchLine] (a)
      {\textbf{Sources}\\specifications, code, traces, simulator and EDA returns};
    \node[datastep,fill=ArchBlueLight,draw=ArchBlue,right=of a] (b)
      {\textbf{Curate}\\identity, units, conditions, failures, provenance};
    \node[datastep,fill=ArchGreenLight,draw=ArchGreen,right=of b] (c)
      {\textbf{Represent}\\graphs, sequences, features, embeddings, structured state};
    \node[datastep,fill=ArchGoldLight,draw=ArchGold,right=of c] (d)
      {\textbf{Use}\\retrieve, predict, generate, optimize, and check};
    \node[datastep,fill=ArchRedLight,draw=ArchRed,right=of d] (e)
      {\textbf{Refresh}\\new measurements, failures, drift, changed assumptions};
    \draw[-{Stealth},ArchMuted,thick] (a)--(b);
    \draw[-{Stealth},ArchMuted,thick] (b)--(c);
    \draw[-{Stealth},ArchMuted,thick] (c)--(d);
    \draw[-{Stealth},ArchMuted,thick] (d)--(e);
  \end{tikzpicture}
  \end{center}
  \vspace{0.12cm}
  \begin{center}
    \begin{tikzpicture}
      \node[rounded corners=3pt,fill=ArchBlueLight,draw=ArchBlue,
        text width=12.8cm,align=center,minimum height=0.72cm,
        font=\small\bfseries,text=ArchBlue]
        {An embedding is one learned representation, not a substitute for project identity, conditions, or tool-facing artifacts.};
    \end{tikzpicture}
  \end{center}
\end{frame}

% 22
\section{Methods}

\begin{frame}{Role comes before method}
  \claim{Do not collapse an engineering job, a method family, and an implementation into ``the AI.''}
  \begin{center}
  \begin{tikzpicture}[node distance=0.18cm]
    \tikzset{s/.style={rounded corners=2pt,minimum width=13.4cm,minimum height=0.73cm,
      align=left,inner xsep=10pt,font=\small}}
    \node[s,fill=ArchPanel,draw=ArchLine] (a) {\textbf{1. Engineering role}\hspace{0.5cm} propose, estimate, select, check, operate, interpret};
    \node[s,fill=ArchBlueLight,draw=ArchBlue,below=of a] (b) {\textbf{2. Need for an added method}\hspace{0.5cm} direct tools or existing analysis may already suffice};
    \node[s,fill=ArchGreenLight,draw=ArchGreen,below=of b] (c) {\textbf{3. Method family}\hspace{0.5cm} generation, prediction, optimization, or a combination};
    \node[s,fill=ArchGoldLight,draw=ArchGold,below=of c] (d) {\textbf{4. Implementation}\hspace{0.5cm} classical, learned, language-model based, or hybrid};
  \end{tikzpicture}
  \end{center}
\end{frame}

% 22
\begin{frame}{Use the smallest sufficient approach}
  \claim{Choose the least costly approach that resolves the named bottleneck.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.36cm]
    \tikzset{m/.style={rounded corners=3pt,draw=ArchLine,minimum width=3.0cm,
      minimum height=1.15cm,align=center,font=\small}}
    \node[m,fill=ArchGreenLight,draw=ArchGreen] (a) {\textbf{Direct analysis}\\closed form, roofline};
    \node[m,fill=ArchGreenLight,draw=ArchGreen,right=of a] (b) {\textbf{Enumeration}\\when the space fits};
    \node[m,fill=ArchGreenLight,draw=ArchGreen,right=of b] (c) {\textbf{Established tools}\\compiler, simulator, EDA};
    \node[m,fill=ArchBlueLight,draw=ArchBlue,right=of c] (d) {\textbf{Added method}\\generator, predictor,\\or optimizer};
  \end{tikzpicture}
  \end{center}
  \vspace{0.4cm}
  \bigsep{An added generator, predictor, or optimizer is optional when direct analysis or tools suffice.}
\end{frame}

% 23
\begin{frame}{Three method families}
  \claim{Start from what is missing, not from a fixed prediction--generation--optimization sequence.}
  \begin{center}
  \begin{tikzpicture}
    \coordinate (g) at (0,1.6);
    \coordinate (p) at (-3.05,-1.05);
    \coordinate (o) at (3.05,-1.05);
    \fill[ArchBlueLight,opacity=.9] (g) circle (1.2cm);
    \fill[ArchGreenLight,opacity=.9] (p) circle (1.2cm);
    \fill[ArchGoldLight,opacity=.9] (o) circle (1.2cm);
    \draw[ArchBlue,thick] (g) circle (1.2cm);
    \draw[ArchGreen,thick] (p) circle (1.2cm);
    \draw[ArchGold,thick] (o) circle (1.2cm);
    \node[align=center,font=\bfseries] at (g) {Generation\\\footnotesize missing candidate\\or artifact};
    \node[align=center,font=\bfseries] at (p) {Prediction\\\footnotesize expensive outcome\\or transition};
    \node[align=center,font=\bfseries] at (o) {Optimization\\\footnotesize scarce evaluations\\or choices};
    \node[rounded corners=3pt,fill=white,draw=ArchLine,align=center,font=\scriptsize] at (0,0.25)
      {combine only when handoffs earn their cost};
  \end{tikzpicture}
  \end{center}
\end{frame}

% 24
\begin{frame}{Generation expands; verification narrows}
  \claim{Attempted, executable, test-passing, and useful are different conditions.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.12cm]
    \node[rounded corners=2pt,
      minimum width=11.6cm,minimum height=0.58cm,fill=ArchBlueLight,draw=ArchBlue] (a)
      {\textbf{100 generated kernels}};
    \node[rounded corners=2pt,
      minimum width=9.5cm,minimum height=0.58cm,fill=ArchPanel,draw=ArchLine,below=of a] (b)
      {83 compile};
    \node[rounded corners=2pt,
      minimum width=7.7cm,minimum height=0.58cm,fill=ArchPanel,draw=ArchLine,below=of b] (c)
      {73 execute};
    \node[rounded corners=2pt,
      minimum width=7.0cm,minimum height=0.58cm,fill=ArchGreenLight,draw=ArchGreen,below=of c] (d)
      {50 match KernelBench's reference outputs on five test inputs};
  \end{tikzpicture}
  \end{center}
  \vspace{0.16cm}
  \begin{center}\color{ArchRed}\bfseries Passing five tests does not establish correctness outside those inputs.\end{center}
  \source{KernelBench, Ouyang et al. (2025), separate 100-kernel error-analysis sample. Per-model one-shot results in the paper show the same attempted $\rightarrow$ correct $\rightarrow$ correct-and-faster narrowing.}
\end{frame}

% 25
\begin{frame}{Prediction trades cost for a support assumption}
  \claim{A cheap predictor helps only where its error is acceptable for the decision it screens.}
  \begin{center}
  \begin{tikzpicture}
    \fill[ArchGreenLight] (-6,-1.8) rectangle (0,2.3);
    \fill[ArchRedLight] (0,-1.8) rectangle (6,2.3);
    \draw[ArchLine] (-6,0) -- (6,0);
    \draw[ArchLine] (0,-1.8) -- (0,2.3);
    \draw[ArchBlue,thick] plot[smooth] coordinates {(-5.5,-.8) (-4,-.1) (-2.5,.4) (-1,.9) (0,1.0) (1,1.1) (2.5,1.3) (4,1.5) (5.5,1.7)};
    \draw[ArchGreen,thick,dashed] plot[smooth] coordinates {(-5.5,-.7) (-4,-.2) (-2.5,.5) (-1,.8) (0,.95) (1,.65) (2.5,.1) (4,-.2) (5.5,-.5)};
    \node[font=\small\bfseries,text=ArchGreen,align=center] at (-3,1.85)
      {calibrated workload, design,\\and tool regime};
    \node[font=\small\bfseries,text=ArchRed,align=center] at (3,1.85)
      {changed workload, process,\\compiler, or memory regime};
    \node[font=\small,align=center] at (-3,-1.25) {prediction agrees with\\stronger observations};
    \node[font=\small,align=center] at (3,-1.25) {unchanged predictor\\can mis-rank candidates};
    \node[font=\scriptsize,text=ArchBlue] at (4.9,1.85) {predictor};
    \node[font=\scriptsize,text=ArchGreen] at (4.9,-.55) {observed};
  \end{tikzpicture}
  \end{center}
  \source{Constructed teaching illustration; Chapter 5 develops calibration and support limits.}
\end{frame}

% 26
\begin{frame}{A world model is a learned transition model}
  \claim{It predicts a sequence of future states from a represented state and proposed actions. Here it supports prediction or planning; it is not the live project record or a verification tool.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.42cm]
    \tikzset{worldbox/.style={rounded corners=3pt,text width=3.4cm,minimum height=1.15cm,
      align=center,font=\small}}
    \node[worldbox,fill=ArchBlueLight,draw=ArchBlue] (a)
      {\textbf{Represented state + action}\\current state,\\constraints, proposed move};
    \node[worldbox,fill=ArchGoldLight,draw=ArchGold,right=of a] (b)
      {\textbf{Learned}\\\textbf{transition model}\\predicts within stated scope};
    \node[worldbox,fill=ArchRedLight,draw=ArchRed,right=of b] (c)
      {\textbf{Predicted future}\\next state or state\\sequence + uncertainty};
    \node[worldbox,fill=ArchGreenLight,draw=ArchGreen,below=0.55cm of c] (d)
      {\textbf{Tool observation}\\checks the prediction\\differently};
    \draw[-{Stealth},ArchMuted,thick] (a)--(b);
    \draw[-{Stealth},ArchMuted,thick] (b)--(c);
    \draw[-{Stealth},ArchGreen,thick] (d)--(c);
  \end{tikzpicture}
  \end{center}
  \vspace{0.15cm}
  \begin{center}\small The observation can test model support; it does not make the model authoritative.\end{center}
\end{frame}

% 27
\begin{frame}{Optimization can allocate the next evaluation}
  \claim{Optimization selects or improves candidates under an objective, constraints, and a stopping rule. For costly black-box objectives, it must also decide what to measure next.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.25cm]
    \node[rounded corners=3pt,fill=ArchPanel,draw=ArchLine,text width=2.55cm,minimum height=1.2cm,align=center] (a)
      {\textbf{Candidate space}\\many\\legal choices};
    \node[rounded corners=3pt,fill=ArchBlueLight,draw=ArchBlue,text width=2.55cm,minimum height=1.2cm,align=center,right=of a] (b)
      {\textbf{Search state}\\observations + uncertainty};
    \node[rounded corners=3pt,fill=ArchGoldLight,draw=ArchGold,text width=2.55cm,minimum height=1.2cm,align=center,right=of b] (c)
      {\textbf{Next evaluation}\\chosen for information or value};
    \node[rounded corners=3pt,fill=ArchGreenLight,draw=ArchGreen,text width=2.55cm,minimum height=1.2cm,align=center,right=of c] (d)
      {\textbf{Tool return}\\updates the search state};
    \draw[-{Stealth},ArchBlue,thick] (a)--(b);
    \draw[-{Stealth},ArchBlue,thick] (b)--(c);
    \draw[-{Stealth},ArchBlue,thick] (c)--(d);
    \coordinate (dloop) at ($(d.south)+(0,-0.65cm)$);
    \coordinate (bloop) at ($(b.south)+(0,-0.65cm)$);
    \draw[ArchRed,thick,dashed] (d.south) -- (dloop);
    \draw[ArchRed,thick,dashed] (dloop) -- node[below,font=\small,text=ArchRed]{another run only if justified} (bloop);
    \draw[-{Stealth},ArchRed,thick,dashed] (bloop) -- (b.south);
  \end{tikzpicture}
  \end{center}
\end{frame}

% 28
\begin{frame}{Method handoffs must earn their cost}
  \claim{This is one possible combination; no fixed order is implied.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.6cm]
    \tikzset{c/.style={rounded corners=3pt,draw=ArchLine,minimum width=2.75cm,text width=2.55cm,
      minimum height=1.0cm,align=center,font=\small}}
    \node[c,fill=ArchBlueLight,draw=ArchBlue] (g) {\textbf{Generate}\\a legal candidate};
    \node[c,fill=ArchGreenLight,draw=ArchGreen,right=of g] (p) {\textbf{Predict}\\screen outcome\\or cost};
    \node[c,fill=ArchGoldLight,draw=ArchGold,right=of p] (o) {\textbf{Optimize}\\choose the\\next run};
    \node[c,fill=ArchRedLight,draw=ArchRed,right=of o] (t) {\textbf{Direct evaluation}\\invoke the\\decisive tool};
    \draw[-{Stealth},ArchMuted,thick] (g)--(p);
    \draw[-{Stealth},ArchMuted,thick] (p)--(o);
    \draw[-{Stealth},ArchMuted,thick] (o)--(t);
  \end{tikzpicture}
  \end{center}
  \vspace{0.08cm}
  \begin{center}\small Handoffs carry assumptions about representation, support, and budget. Tool returns still need qualification.\end{center}
  \vspace{0.08cm}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \tagbox{ArchGreenLight}{ArchGreen}{Justified}
      \begin{itemize}
        \item each stage removes more cost than it adds
        \item rejected candidates and failures stay visible
      \end{itemize}
    \column{0.48\textwidth}
      \tagbox{ArchRedLight}{ArchRed}{Not justified}
      \begin{itemize}
        \item more components without a named bottleneck
        \item a cheap proxy replaces the decisive check
      \end{itemize}
  \end{columns}
\end{frame}

% 29
\section{Environments}

\begin{frame}{An environment is more than a simulator}
  \claim{An environment defines how a design request becomes a recorded tool outcome.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.5cm]
    \tikzset{envcol/.style={rounded corners=3pt,text width=3.7cm,minimum height=2.65cm,
      align=left,inner xsep=9pt,font=\small}}
    \node[envcol,fill=ArchGoldLight,draw=ArchGold] (a)
      {\textbf{Inputs}\\[2pt]question + workload\\permitted actions\\constraints + metrics};
    \node[envcol,fill=ArchBlueLight,draw=ArchBlue,right=of a] (b)
      {\textbf{Environment}\\[2pt]check request\\invoke the tool\\parse status\\record cost + provenance};
    \node[envcol,fill=ArchGreenLight,draw=ArchGreen,right=of b] (c)
      {\textbf{Recorded outcome}\\[2pt]invalid request\\execution failure\\incomplete or complete return\\raw output + run record};
    \draw[-{Stealth},ArchMuted,thick] (a)--(b);
    \draw[-{Stealth},ArchMuted,thick] (b)--(c);
  \end{tikzpicture}
  \end{center}
  \vspace{0.2cm}
  \begin{center}\small Define permitted actions, fixed context, expected outputs, cost, lineage, and status before execution.\end{center}
\end{frame}

% 30
\begin{frame}{Identity must survive every tool}
  \claim{A successful command is not enough to reconstruct a result.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.38cm and 0.58cm]
    \tikzset{artifactbox/.style={rounded corners=3pt,text width=3.25cm,minimum height=0.95cm,
      align=center,font=\small}}
    \node[artifactbox,fill=ArchBlueLight,draw=ArchBlue] (s1) {\textbf{Software source S2}\\content hash + version};
    \node[artifactbox,fill=ArchBlueLight,draw=ArchBlue,right=of s1] (s2) {\textbf{Compiled binary B2}\\compiler + settings};
    \node[artifactbox,fill=ArchGoldLight,draw=ArchGold,below=of s1] (h1) {\textbf{Hardware source H3}\\content hash + version};
    \node[artifactbox,fill=ArchGoldLight,draw=ArchGold,right=of h1] (h2) {\textbf{Elaborated RTL R3}\\generator + settings};
    \node[artifactbox,fill=ArchGreenLight,draw=ArchGreen,right=0.9cm of s2.south east,anchor=west] (e)
      {\textbf{Evaluation E7}\\B2 + R3 + tool versions\\+ workload + conditions};
    \draw[-{Stealth},ArchMuted,thick] (s1)--(s2);
    \draw[-{Stealth},ArchMuted,thick] (h1)--(h2);
    \draw[-{Stealth},ArchMuted,thick] (s2.east)--(e.west);
    \draw[-{Stealth},ArchMuted,thick] (h2.east)--(e.west);
  \end{tikzpicture}
  \end{center}
  \vspace{0.25cm}
  \bigsep{Artifact identity = content + production lineage.\\
    Evaluation identity = exact artifacts + tool versions + workload + conditions.}
\end{frame}

% 31
\begin{frame}{A return is not yet a measurement}
  \claim{A value becomes a measurement only when its identity, conditions, status, and limits are bound to it.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.18cm]
    \tikzset{measurestage/.style={rounded corners=3pt,minimum width=2.45cm,text width=2.15cm,
      minimum height=1.35cm,align=center,inner xsep=3pt,font=\small}}
    \node[measurestage,fill=ArchPanel,draw=ArchLine] (a) {\textbf{Tool return}\\status, logs,\\artifacts};
    \node[measurestage,fill=ArchGreenLight,draw=ArchGreen,right=of a] (b) {\textbf{Bind identity}\\candidate, property,\\unit};
    \node[measurestage,fill=ArchBlueLight,draw=ArchBlue,right=of b] (c) {\textbf{Bind}\\\textbf{conditions}\\tool + version, config,\\artifact status};
    \node[measurestage,fill=ArchGoldLight,draw=ArchGold,right=of c] (d) {\textbf{State limits}\\variation, error,\\or known bound};
    \node[measurestage,fill=ArchRedLight,draw=ArchRed,right=of d] (e) {\textbf{Compare + interpret}\\baseline, meaning,\\action};
    \draw[-{Stealth},ArchMuted,thick] (a)--(b);
    \draw[-{Stealth},ArchMuted,thick] (b)--(c);
    \draw[-{Stealth},ArchMuted,thick] (c)--(d);
    \draw[-{Stealth},ArchMuted,thick] (d)--(e);
  \end{tikzpicture}
  \end{center}
  \vspace{0.48cm}
  \bigsep{If qualification fails, retain an execution return, not a measurement for comparison.}
\end{frame}

% 32
\section{Verification, feedback, and learning}

\begin{frame}{Formal and simulation answer different questions}
  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      \tagbox{ArchBlueLight}{ArchBlue}{Formal}
      \begin{itemize}
        \item proves a stated property or finds a counterexample
        \item under a model and assumptions
        \item may be inconclusive or time out
      \end{itemize}
    \column{0.47\textwidth}
      \tagbox{ArchGreenLight}{ArchGreen}{Simulation}
      \begin{itemize}
        \item observes exercised behavior
        \item under selected stimuli and configuration
        \item cannot exhaust a modern design
      \end{itemize}
  \end{columns}
  \vspace{0.38cm}
  \bigsep{Neither check repairs an incomplete requirement or system boundary.}
\end{frame}

% 33
\begin{frame}{Another model is not an independent check}
  \claim{Independence comes from a materially different observation path, not another fluent answer.}
  \begin{center}
    \includegraphics[width=0.87\linewidth,height=4.7cm,keepaspectratio]{../book/contents/chapters/07-feedback/images/ch7_independent_checks.pdf}
  \end{center}
\end{frame}

\begin{frame}{Feedback must update the right thing}
  \claim{An invalid execution return goes back to the environment. A qualified or verified result can direct feedback to the candidate, method support, project state, or reusable knowledge; each update receives an appropriate recheck.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.25cm and 0.42cm]
    \tikzset{feedbackbox/.style={rounded corners=3pt,text width=2.75cm,
      minimum height=1.25cm,align=center,font=\small}}
    \node[feedbackbox,fill=ArchBlueLight,draw=ArchBlue] (a)
      {\textbf{Candidate repair}\\change the artifact or configuration};
    \node[feedbackbox,fill=ArchPanel,draw=ArchLine,right=of a] (b)
      {\textbf{Method support}\\recalibrate or narrow supported use};
    \node[feedbackbox,fill=ArchGreenLight,draw=ArchGreen,right=of b] (c)
      {\textbf{Project state}\\update constraints, identity, or tool status};
    \node[feedbackbox,fill=ArchGoldLight,draw=ArchGold,right=of c] (d)
      {\textbf{Reusable learning}\\generalize only after repeated independent checks};
  \end{tikzpicture}
  \end{center}
  \vspace{0.4cm}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \tagbox{ArchGreenLight}{ArchGreen}{Learn when justified}
      \begin{itemize}
        \item enough qualified observations
        \item held-out or independent recheck
      \end{itemize}
    \column{0.48\textwidth}
      \tagbox{ArchRedLight}{ArchRed}{Do not silently learn}
      \begin{itemize}
        \item one anomalous return
        \item changed workload or tool regime
      \end{itemize}
  \end{columns}
\end{frame}

% 35
\section{Running the design loop}

\begin{frame}{One design loop separates four judgments}
  \claim{The prospective XR cache record stops before execution. A separate executed synthetic GEMM array study shows how a design loop separates outcome, contribution, mechanism, and stopping.}
  \begin{center}
    \includegraphics[width=0.91\linewidth,height=4.85cm,keepaspectratio]{../book/contents/chapters/08-running-the-loop/images/ch8_design_loop_workflow.pdf}
  \end{center}
  \source{Chapter 8. The figure describes the retained SCALE-Sim array study, not the prospective XR cache record or a complete Lighthouse result.}
\end{frame}

% 35
\begin{frame}{The executed array study produced a local tie}
  \claim{Frozen local score = total modeled cycles $\div$ average layer utilization; lower is better. It is a study-local ordering rule, not a physical measurement.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.28cm]
    \tikzset{row/.style={rounded corners=3pt,minimum width=12.7cm,minimum height=0.88cm,align=left,inner xsep=10pt,font=\small}}
    \node[row,fill=ArchBlueLight,draw=ArchBlue] (a)
      {\textbf{Model arm}\hspace{0.45cm} 4 simulator calls \hspace{0.45cm} score 33,782 \hspace{0.45cm} 32$\times$32 and 16$\times$64 tied};
    \node[row,fill=ArchGreenLight,draw=ArchGreen,below=of a] (b)
      {\textbf{Fixed heuristic}\hspace{0.45cm} 4 simulator calls \hspace{0.45cm} score 33,782 \hspace{0.45cm} 32$\times$32 and 16$\times$64 tied};
    \node[rounded corners=4pt,fill=ArchRedLight,draw=ArchRed,minimum width=12.7cm,minimum height=1.0cm,
      font=\small\bfseries,align=center,below=0.35cm of b] (c)
      {No evaluated nonbaseline cleared the margin.\\Technical recommendation: retain 32$\times$32 for the evaluated set and stop.\\Accountable confirmation remains pending; no architecture commitment.};
  \end{tikzpicture}
  \end{center}
  \vspace{0.12cm}
  \begin{center}\small\color{ArchGold}
    Post-run check: 16$\times$64 had twice the baseline's modeled DRAM-word writes, omitted from the frozen score.
  \end{center}
  \source{Retained SCALE-Sim 3.0.0 study in Chapter 8. Synthetic three-layer GEMM, not an XR trace or Lighthouse result; no architecture commitment or silicon measurement.}
\end{frame}

% 36
\begin{frame}{The failed explanation is also a result}
  \claim{In this general matrix multiplication (GEMM) contrast, the local comparison survives even though the proposed explanation fails.}
  \begin{columns}[T,onlytextwidth]
    \column{0.60\textwidth}
      \tagbox{ArchBlueLight}{ArchBlue}{Setup}
      \smallskip

      \small In a weight-stationary dataflow, weights stay in the array.
      $K$ maps to array height, $N$ to array width, and $M$ streams through.
      \vspace{0.35cm}

      \begin{center}
      \begin{tikzpicture}[node distance=0.65cm]
        \tikzset{arrayshape/.style={rounded corners=3pt,minimum width=3.1cm,
          minimum height=1.15cm,align=center,font=\small}}
        \node[arrayshape,fill=ArchBlueLight,draw=ArchBlue] (a)
          {\textbf{16$\times$64 array}\\height 16, width 64};
        \node[arrayshape,fill=ArchGoldLight,draw=ArchGold,right=of a] (b)
          {\textbf{64$\times$16 array}\\height 64, width 16};
      \end{tikzpicture}
      \end{center}
      \vspace{0.22cm}

      \begin{center}\bfseries Matched workload contrast: swap $M$ and $N$; keep $K$ unchanged.\end{center}
    \column{0.36\textwidth}
      \vspace{0.2cm}
      \tagbox{ArchRedLight}{ArchRed}{Prediction}
      \smallskip

      Transposing $M$ and $N$ would reverse the winner.
      \vspace{0.28cm}

      \tagbox{ArchGreenLight}{ArchGreen}{Observed}
      \smallskip

      16$\times$64 ranks ahead of 64$\times$16 in this contrast.
      \vspace{0.28cm}

      \textbf{Keep the observed ordering. Reject the proposed mechanism.}
  \end{columns}
  \source{SCALE-Sim Project v3.0.0 source and retained Chapter 8 execution records. This synthetic GEMM contrast is separate from the prospective XR record.}
\end{frame}

% 37
\section{Patterns and transfer}

\begin{frame}{A new decision reopens the claim}
  \claim{Study structure may transfer; a local conclusion does not transfer automatically.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.55cm]
    \tikzset{transferbox/.style={rounded corners=3pt,minimum width=3.5cm,minimum height=1.45cm,align=center}}
    \node[transferbox,fill=ArchBlueLight,draw=ArchBlue] (a) {\textbf{Source study}\\synthetic GEMM\\SCALE-Sim score};
    \node[transferbox,fill=ArchGoldLight,draw=ArchGold,right=of a] (b) {\textbf{Changed target}\\real XR workload\\software + memory +\\power, performance, area};
    \node[transferbox,fill=ArchRedLight,draw=ArchRed,right=of b] (c) {\textbf{New obligations}\\recalibrate, rerun,\\add implementation checks};
    \draw[-{Stealth},ArchMuted,thick] (a)--(b);
    \draw[-{Stealth},ArchMuted,thick] (b)--(c);
  \end{tikzpicture}
  \end{center}
  \vspace{0.35cm}
  \begin{center}\small Workload, software, hardware, tools, process assumptions, and decision scope are part of the result.\end{center}
\end{frame}

\begin{frame}{What transfers is structure, not the answer}
  \claim{Across design problems, reuse the disciplined parts of the study and re-establish the facts that changed.}
  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      \tagbox{ArchGreenLight}{ArchGreen}{Often reusable}
      \begin{itemize}
        \item question and comparison template
        \item environment interface and run record
        \item method role and independent checks
        \item cost accounting and stopping logic
      \end{itemize}
    \column{0.47\textwidth}
      \tagbox{ArchRedLight}{ArchRed}{Must be re-established}
      \begin{itemize}
        \item workload and software behavior
        \item model calibration and support
        \item tool, process, and physical conditions
        \item evidence threshold and decision authority
      \end{itemize}
  \end{columns}
  \vspace{0.3cm}
  \bigsep{Transfer a method only as far as its assumptions and checks still hold.}
\end{frame}

\begin{frame}{Four patterns recur across the stack}
  \claim{The artifacts differ from software to circuits, but the architecture problems repeat.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.28cm and 0.35cm]
    \tikzset{patternbox/.style={rounded corners=3pt,text width=5.65cm,
      minimum height=1.25cm,align=left,inner xsep=8pt,font=\small}}
    \node[patternbox,fill=ArchBlueLight,draw=ArchBlue] (a)
      {\textbf{Costly decisive evaluation}\\Use cheap bounds and predictors to decide which candidates deserve stronger tools.};
    \node[patternbox,fill=ArchGreenLight,draw=ArchGreen,right=of a] (b)
      {\textbf{Coupled choices}\\Hardware, software, tools, and workloads move together; preserve the complete comparison.};
    \node[patternbox,fill=ArchGoldLight,draw=ArchGold,below=of a] (c)
      {\textbf{Partial observability}\\Represent conditions and uncertainty; do not treat an absent measurement as a favorable one.};
    \node[patternbox,fill=ArchRedLight,draw=ArchRed,right=of c] (d)
      {\textbf{Verification effort follows failure cost}\\Generate cheaply when possible, but scale checks with the cost of a wrong commitment.};
  \end{tikzpicture}
  \end{center}
\end{frame}

% 40
\section{Evaluation}

\begin{frame}{Evaluate matched complete systems}
  \claim{Matched complete systems can estimate a workflow-package adoption effect. A model score cannot.}
  \begin{center}
    \tagbox{ArchPanel}{ArchLine}{Same decision, starting information, evaluator-owned tools and checks, cost window, and stopping rule}
  \end{center}
  \vspace{0.15cm}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \begin{beamercolorbox}[rounded=true,sep=0.15cm]{block body}
        \color{ArchBlue}\bfseries AI-assisted design system
      \end{beamercolorbox}
      \begin{itemize}
        \item internal models, methods, and tools
        \item operator expertise and intervention
        \item failures and all resource costs retained
      \end{itemize}
    \column{0.48\textwidth}
      \begin{beamercolorbox}[rounded=true,sep=0.15cm]{block body}
        \color{ArchGold}\bfseries Strong comparison system
      \end{beamercolorbox}
      \begin{itemize}
        \item established heuristic or human process
        \item practitioner expertise and intervention
        \item failures and all resource costs retained
      \end{itemize}
  \end{columns}
  \vspace{0.03cm}
  \begin{center}
    \begin{tikzpicture}
      \node[rounded corners=3pt,fill=ArchBlueLight,draw=ArchBlue,text width=12.7cm,
        align=center,minimum height=1.08cm,font=\small\bfseries,text=ArchBlue]
        {Compare paired outcome--cost results across declared budgets and repeats. Controlled replacement or ablation tests a component's contribution, but interactions may leave attribution unresolved. Neither assessment authorizes architecture commitment.};
    \end{tikzpicture}
  \end{center}
\end{frame}

% 39
\begin{frame}{Metrics people forget to count}
  \claim{Six metric groups support four portable judgments: architecture result; coordination and decision quality; total human and machine cost; and reliability across repeated, changed, and adversarial conditions.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.08cm]
    \tikzset{metric/.style={rounded corners=2pt,minimum width=13.5cm,minimum height=0.53cm,
      align=left,inner xsep=7pt,font=\fontsize{8.4}{10}\selectfont}}
    \node[metric,fill=ArchBlueLight,draw=ArchBlue] (a) {\textbf{Architecture}\hspace{0.45cm} performance, power, area, reliability, software and product constraints};
    \node[metric,fill=ArchPanel,draw=ArchLine,below=of a] (b) {\textbf{Work performed}\hspace{0.45cm} candidates, model calls, tokens, tool calls by type, unique simulator or implementation runs};
    \node[metric,fill=ArchPanel,draw=ArchLine,below=of b] (c) {\textbf{Resources + people}\hspace{0.45cm} wall/queue time, compute, energy, licenses, data preparation, training, setup, review, repair};
    \node[metric,fill=ArchGreenLight,draw=ArchGreen,below=of c] (d) {\textbf{Experimental validity}\hspace{0.45cm} task/workload coverage, paired repeats/seeds, dispersion, leakage/contamination checks};
    \node[metric,fill=ArchGoldLight,draw=ArchGold,below=of d] (e) {\textbf{Evidence + decision}\hspace{0.45cm} qualification, uncertainty, check coverage, supported or unresolved status, reopening};
    \node[metric,fill=ArchRedLight,draw=ArchRed,below=of e] (f) {\textbf{Failures + control}\hspace{0.45cm} invalid actions, retries, timeouts, recovery, budget violations, unsafe changes};
  \end{tikzpicture}
  \end{center}
  \vspace{0.15cm}
  \begin{center}\color{ArchRed}\bfseries Ten tool calls do not have one common cost.\end{center}
\end{frame}

% 40
\begin{frame}{Stress-test failures; red-team attacks}
  \claim{Fault injection probes resilience. Red teaming predeclares the asset, adversary, access, attack goal, success criteria, containment, and recovery.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.35cm and 0.4cm]
    \tikzset{q/.style={rounded corners=3pt,text width=5.55cm,minimum height=1.45cm,align=left,inner xsep=8pt,font=\small}}
    \node[q,fill=ArchRedLight,draw=ArchRed] (a) {\textbf{Artifact attack}\\hidden logic, backdoors, unsafe\\interfaces, physical side effects};
    \node[q,fill=ArchGoldLight,draw=ArchGold,right=of a] (b) {\textbf{Evaluator attack}\\weakened tests, proxy gaming, omitted conditions, contaminated data};
    \node[q,fill=ArchPanel,draw=ArchLine,below=of a] (c) {\textbf{Workflow faults}\\silent retries, hidden failures,\\uncontrolled tool or model spending};
    \node[q,fill=ArchBlueLight,draw=ArchBlue,right=of c] (d) {\textbf{Authority attack}\\changed requirements, unauthorized waivers, recommendation treated as approval};
  \end{tikzpicture}
  \end{center}
  \source{Chapter 10. Check final artifacts and downstream tool stages, not only source RTL; record attack success, detection, containment, recovery cost, and residual risk.}
\end{frame}

% 41
\section{The architect}

\begin{frame}{What remains architectural}
  \claim{The architect's role is a positive technical contribution, not whatever automation has not absorbed.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.18cm]
    \tikzset{architecttask/.style={rounded corners=3pt,minimum width=2.35cm,text width=2.05cm,
      minimum height=1.35cm,align=center,inner xsep=3pt,font=\small}}
    \node[architecttask,fill=ArchBlueLight,draw=ArchBlue] (a) {\textbf{Frame}\\question, boundary, tradeoffs};
    \node[architecttask,fill=ArchPanel,draw=ArchLine,right=of a] (b) {\textbf{Represent}\\state,\\constraints,\\alternatives};
    \node[architecttask,fill=ArchGreenLight,draw=ArchGreen,right=of b] (c) {\textbf{Choose}\\methods, tools, checks};
    \node[architecttask,fill=ArchGoldLight,draw=ArchGold,right=of c] (d) {\textbf{Interpret}\\results, limits, conflicts};
    \node[architecttask,fill=ArchRedLight,draw=ArchRed,right=of d] (e) {\textbf{Recommend}\\action, limits, reopening};
    \draw[-{Stealth},ArchMuted,thick] (a)--(b);
    \draw[-{Stealth},ArchMuted,thick] (b)--(c);
    \draw[-{Stealth},ArchMuted,thick] (c)--(d);
    \draw[-{Stealth},ArchMuted,thick] (d)--(e);
  \end{tikzpicture}
  \end{center}
  \vspace{0.48cm}
  \bigsep{The architect produces a supported technical recommendation. Named commitment authority decides whether the design advances.}
\end{frame}

\begin{frame}{The field now has a research agenda}
  \claim{The next advances must improve design capability and the discipline for judging it.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.12cm]
    \tikzset{agenda/.style={rounded corners=3pt,minimum width=12.9cm,
      minimum height=0.6cm,align=left,inner xsep=8pt,font=\fontsize{8.8}{10.2}\selectfont}}
    \node[agenda,fill=ArchBlueLight,draw=ArchBlue] (a)
      {\textbf{Represent}\hspace{0.5cm} Build architecture data and state representations that preserve meaning across tools and time.};
    \node[agenda,fill=ArchGreenLight,draw=ArchGreen,below=of a] (b)
      {\textbf{Compose}\hspace{0.5cm} Learn when prediction, generation, optimization, and classical analysis should cooperate.};
    \node[agenda,fill=ArchGoldLight,draw=ArchGold,below=of b] (c)
      {\textbf{Verify}\hspace{0.5cm} Develop independent checks that scale with generated candidates and physical consequence.};
    \node[agenda,fill=ArchRedLight,draw=ArchRed,below=of c] (d)
      {\textbf{Evaluate}\hspace{0.5cm} Compare complete systems on architecture results, total cost, robustness, and decision quality.};
    \node[agenda,fill=ArchPanel,draw=ArchLine,below=of d] (e)
      {\textbf{Judge}\hspace{0.5cm} Establish what the result supports, state the technical recommendation, and name who can commit.};
  \end{tikzpicture}
  \end{center}
  \vspace{0.08cm}
  \begin{center}\color{ArchBlue}\small\bfseries The goal is not autonomous output. It is better architecture work that can be defended.\end{center}
\end{frame}

% 47
\begin{frame}{Three judgments that remain separate}
  \claim{A tool return becomes a measurement only after its identity, conditions, status, and limits are bound to it.}
  \begin{center}
  \begin{tikzpicture}[node distance=0.22cm]
    \tikzset{closingbox/.style={rounded corners=4pt,minimum width=13.5cm,minimum height=0.78cm,align=center,font=\fontsize{14}{16}\selectfont\bfseries}}
    \node[closingbox,fill=ArchBlueLight,draw=ArchBlue] (a) {Qualified measurements and checks support a technical recommendation.};
    \node[closingbox,fill=ArchGreenLight,draw=ArchGreen,below=of a] (b) {A matched evaluation separately asks whether the complete AI-assisted approach helped.};
    \node[closingbox,fill=ArchRedLight,draw=ArchRed,below=of b] (c) {The named commitment authority decides whether a particular design advances.};
  \end{tikzpicture}
  \end{center}
  \vspace{0.15cm}
  \begin{center}
    {\fontsize{13}{15}\selectfont\color{ArchInk}
    The recommendation and complete-approach assessment answer different questions.\\
    Neither authorizes commitment, and the assessment is not a prerequisite for advancing a design.\par}
  \end{center}
\end{frame}

\end{document}
