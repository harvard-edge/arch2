# Fabricated claim trace

Compiled 3 September 2026 in worktree `Arch2-provenance`, branch
`fix/fabricated-receipts`. Nothing in the manuscript or the website has been
changed. This is the trace only; the repairs are the author's to approve.

## The finding in one line

Three files in `data/source-receipts/` contain values that no tool produced.
All three are published on the live website. One of them is cited in the book
to three real papers that did not produce its numbers.

---

## Source 1 · `testbench_vacuity_and_judge_calibration.csv`

**Generated by** `data/scrapers/mine_testbench_vacuity_and_judge_bias.py`.
Every numeric column comes from `random.Random(seed)` and `rng.gauss(...)`
(lines 277, 313, 316, 319, 324, 341, 395, 397). No verification tool is invoked
anywhere in the script.

**What the file claims about itself**, in its own header:

```
# Testbench Mutation Vacuity & LLM-as-a-Judge Calibration Provenance Receipt
# Formal Tool Oracle: Cadence JasperGold 2024.09-SP1 (SEC/FPV/FVA) &
#   SymbiYosys 0.44+git (Yosys 0.67+post, SMT Solvers: Boolector 3.2.3,
#   Z3 4.12.5, Bitwuzla 0.6.0)
# Mutation Engine: Certitude-compatible AST Fault Injector & LLM-Mutator
# Total Evaluation Records: 1563 AI-Generated Testbenches & RTL Modules
# Empirical Findings: Mean Line Coverage = 92.87% | Mean Mutation Kill
#   Rate = 37.04% | Vacuity Gap = 55.83%
```

Every tool and version named there is a string literal in the generating
script. The words "Provenance Receipt" and "Empirical Findings" are doing the
opposite of their job.

### Where those numbers reach a reader

| # | File | Line | What it says | Repair |
| --- | --- | --- | --- | --- |
| 1.1 | `book/contents/chapters/07-feedback/07-feedback.qmd` | 163 | Figure caption: "AI testbenches reach $92.9\%$ line coverage but kill only $37.1\%$ of functional faults, exposing a $55.8\%$ Vacuity Gap **[@VerilogEval; @RTLLM; @ThakurEtAl2023VeriGen]**" | **Worst case.** Withdraw or replace. The citation attributes the numbers to three real papers that did not produce them. |
| 1.2 | `book/contents/chapters/07-feedback/07-feedback.qmd` | 163 | Same caption, panel (b): "LLM judges exhibit severe sycophancy ($\text{ECE} = 0.380$, false acceptance rate $86.1\%$), producing a $+2.22\times$ defect escape multiplier" | Withdraw. No judge experiment was run. |
| 1.3 | `www/data.qmd` | 255 | Stat card headline number: **55.8%**, labelled "Testbench Vacuity Gap" | Withdraw from the live site. |
| 1.4 | `www/data.qmd` | 261 | "Line coverage ($92.9\%$) masks low mutation kill rate ($37.1\%$) on AI hardware testbenches." | Withdraw or replace with the transcribed Herdt values. |
| 1.5 | `www/data.qmd` | 453 | "**Core Takeaway:** AI-generated testbenches achieving $>92\%$ line coverage suffer a **$55.8\%$ Vacuity Gap**, failing to kill $>62\%$ of injected functional bugs." | Withdraw. |
| 1.6 | `data/studies/generate_study_packages.py` | 616 | "**Measures** the 55.8% Vacuity Gap ... on 1,563 AI hardware testbenches" | Reword. An explicit measurement claim for synthesised data. |
| 1.7 | `data/studies/generate_study_packages.py` | 620 | "When evaluated against formal mathematical ground truth (JasperGold / SymbiYosys)" | Withdraw. Neither tool ran. |
| 1.8 | `data/studies/08-testbench-vacuity-and-judge-bias/README.md` | 29 | "AI-generated testbenches achieve an average of **$92.9\%$ dynamic line coverage**" | Withdraw or relabel. |
| 1.9 | `data/studies/README.md` | 18 | Study index entry presenting it as a measurement study | Relabel. |

### An extra signal worth noting

The book caption gives **ECE = 0.380**. The generated receipt's own header
gives **ECE = 0.2834**. Two different fabricated values for the same quantity,
which means the number was not even carried consistently from the file it came
from. A reader comparing the caption against the published CSV would find they
disagree.

### The honest replacement already exists

`data/source-receipts/chapter7-testbench-vacuity-mutation.csv` has **no
generating script** and carries a per-row `citation` and `url` to
Herdt, Grosse and Drechsler (ASPDAC 2021) plus OpenHW CORE-V. It reports
88.4% line coverage against a 34.8% mutation kill rate for directed tests, a
53.6-point gap, across four verification tiers rising to 100/100 for formal.

That file is transcribed rather than measured, so it is somebody else's number
honestly attributed. It supports the same argument and it is defensible.

---

## Source 2 · `eda_seed_dispersion_qor_lottery.csv`

**Generated by** `data/scrapers/mine_eda_seed_dispersion.py` via
`_gaussian_noise(key, mean, sigma)`, with `sigma=0.007` for area. The header
names OpenROAD v2.0-13524-g8e91fa4, Yosys 0.67+post and OpenSTA 2.6.0, all
string literals. Stated finding: 3.2%–7.8% seed dispersion.

**Published** at `www/data/observatory/eda_seed_dispersion_qor_lottery.csv`.

### The honest replacement already exists and is better

`data/studies/06-eda-seed-dispersion/openroad_gcd_placement_seed_pilot.csv` is
a real run: 20 seeds, GCD on Nangate45, ORFS pinned at commit `8359fde`,
container pinned by sha256 digest, `subprocess.run(["docker", ...])`, ORFS JSON
parsed for `detailedplace__design__instance__area` and
`detailedplace__timing__setup__ws`. All 20 passed. One floorplan hash across
every run, 20 distinct `GPL_RANDOM_SEED` values.

Measured result: **area spread 0.84%, setup slack spread 3.83%**, a 4.6x ratio.
The keynote already uses this file, not the generated one.

---

## Source 3 · `hardware_ast_complexity_gap.csv`

Per-module values are typed into literal tables in
`data/scrapers/mine_hardware_ast_complexity.py` rather than parsed from source.
Published claim: a **175x** AST complexity gap.

**Published** at `www/data/observatory/hardware_ast_complexity_gap.csv`.

### Contradicted by its own measured replacement

`data/studies/02-ast-complexity-cliff/hardware_ast_complexity_measured.csv`
parses 217 AI-benchmark modules against 1,296 production RTL modules with
pyslang 11.0.0 from pinned commits, per-file SHA-256 retained, and reports
**6.7x** with two disclosed sensitivity checks at 4.77x and 4.27x.

The direction of the claim survives. The magnitude was overstated by more than
an order of magnitude.

---

## Site exposure

All 62 receipts are served publicly from `www/data/observatory/`, including all
three files above, and the figure built from source 1 is served at
`www/images/observatory/fig_testbench_vacuity_and_judge_bias.{png,svg}`.

Anyone who follows the book's own invitation to check its receipts can download
these.

---

## Recommended order of repair

1. **Book caption 1.1 and 1.2.** The false attribution to VerilogEval, RTLLM
   and VeriGen is the only item here that misrepresents other authors' work.
   Fix this first, independently of everything else.
2. **The live site**, items 1.3 to 1.5, plus unpublishing the three CSVs and
   the figure from `www/`.
3. **The measurement claims** in the study packages, items 1.6 and 1.7.
4. **Quarantine** the three files out of `source-receipts/` into a clearly
   marked synthetic directory, and add a validator that fails if a generated
   file appears in `source-receipts/` or if a receipt header names a tool
   version that its generator hardcodes.
5. **Replace where a real measurement is cheap.** Source 2 and source 3 already
   have measured replacements in `data/studies/`. Source 1 does not, but
   `data/scrapers/mine_verilog_eval_mutation_pilot.py` is a genuine
   Icarus-based harness that has never been run to completion.

## What this does not say

The three files are the ones traced here. **53 of the 62 receipts have not yet
been classified.** The pattern (two files from the same late-August scraper
batch, both calling themselves provenance receipts) is reason to finish that
audit before trusting the rest.
