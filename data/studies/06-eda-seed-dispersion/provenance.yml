# Provenance record for 06-eda-seed-dispersion
# Schema: arch2-provenance/v1. One file per data folder. This is the only
# provenance record; anything not listed here has no recorded source.

study: 06-eda-seed-dispersion
title: "Placement seed dispersion"
claim: "Holding design and floorplan fixed and varying only the placement seed moves setup slack 3.83% and instance area 0.84%, so one tool run is a sample, not a measurement."
evidence_class: measured   # mined | measured | transcribed
status: verified

sources:
  - name: "OpenROAD-flow-scripts"
    type: git_repository
    id: 8359fde
    url: https://github.com/The-OpenROAD-Project/OpenROAD-flow-scripts
    note: "Container additionally pinned by SHA-256 digest."
  - name: "GCD design on Nangate45 PDK"
    type: design
    note: "Shipped with ORFS. Run by us, 20 seeds, single-threaded, place target."

datasets:
  - file: openroad_gcd_placement_seed_pilot.csv
    rows: 20
    row_provenance: [floorplan_odb_sha256, placement_odb_sha256, globalplace_json_sha256, detailedplace_json_sha256, stdout_receipt]

produced_by: data/scrapers/run_openroad_gcd_placement_seed_pilot.py
reproduce: "python3 data/scrapers/run_openroad_gcd_placement_seed_pilot.py   # needs Docker, ~20 min"

defects: []

verified: 20/20 runs passed, 1 floorplan hash, 20 distinct placement hashes, area 0.835% and slack 3.836% recompute exactly (2026-09-09)
not_verified: "The 20 Docker runs were not re-executed; retained raw output matches. Result is one design, one PDK, one flow stage. It bounds nondeterminism for this case and must not be cited as a general magnitude."
