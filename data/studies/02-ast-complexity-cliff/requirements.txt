# Reproduction environment for the AST complexity measurement.
# Pinned so a rerun on another machine produces byte-identical results
# (every column except extraction_timestamp).
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# pyslang supplies the SystemVerilog concrete syntax tree and is the only
# dependency the measurement itself needs. matplotlib and numpy are required
# only to redraw the figure.

pyslang==11.0.0
matplotlib==3.11.1
numpy==2.4.6
