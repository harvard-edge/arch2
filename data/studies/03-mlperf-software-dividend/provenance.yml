# Provenance record for 03-mlperf-software-dividend
# Schema: arch2-provenance/v1. One file per data folder. This is the only
# provenance record; anything not listed here has no recorded source.

study: 03-mlperf-software-dividend
title: "Fixed-silicon software dividend"
claim: "On identical frozen silicon, maturing software extracts up to 3.82x additional throughput, while the custom kernel code needed to obtain it grows sharply."
evidence_class: transcribed   # mined | measured | transcribed
status: defective

sources:
  - name: "MLCommons Training and Inference results, 2018-2026"
    type: benchmark_results
    url: https://mlcommons.org/benchmarks/
    note: "Stored result_url values are round-level landing pages and currently 404. Per-submission verification requires the mlcommons/training_results_* repositories."
  - name: "vLLM, TensorRT-LLM, SGLang, TGI tagged releases"
    type: git_repository
    note: "Per-row git_commit and version_tag recorded."

datasets:
  - file: mlperf_longitudinal_software_dividend.csv
    rows: 35
    row_provenance: [submission_id, software_stack_version, result_url]
  - file: inference_kernel_fragmentation.csv
    rows: 26
    row_provenance: [git_commit, github_repo_url, version_tag]

produced_by: data/scrapers/mine_mlperf_software_dividend.py
reproduce: "python3 data/scrapers/mine_mlperf_software_dividend.py"

defects:   # open, not yet repaired
  - where: plot_mlperf_software_dividend_extended.py:411
    what: "'82x kernel proliferation' appears nowhere in the data. Real per-engine growth is vLLM 48.71x, SGLang 43.14x, TGI 23.39x, TensorRT-LLM 8.11x."
  - where: plot_mlperf_software_dividend_extended.py:66-79,243
    what: "Twelve literal arrays supply every plotted coordinate. The script opens the CSV but does not plot from it."
  - where: README.md
    what: "CUTLASS claims (48.7x template growth, 4.2k to 207.7k LOC, 60.4x PTX) have no data. CUTLASS is not an engine in the CSV. The 48.7 figure equals vLLM's growth factor, suggesting misattribution."

verified: 3.82x peak dividend recomputes exactly; TensorRT-LLM 341800 and SGLang 195000 kernel LOC match (2026-09-09)
not_verified: "MLPerf throughput values have not been reconciled against MLCommons published result tables. This is the weakest per-row provenance in the collection."
