services:
  workbench:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: arch2-workbench:latest
    container_name: arch2-workbench
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      - PYTHONUNBUFFERED=1
    command: python3 labs/demo.py
