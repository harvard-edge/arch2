# Chapter 8 Figure Inventory

Active hand-authored figures:

- `ch08-cover-map.svg`: chapter overview mapping prospective vs executed studies, local outcomes, proxy discrepancies, and loop boundaries.
- `fig-array-study-workflow`: one frozen study record leading to four separate
  judgments.
- `fig-array-mapping`: the SCALE-Sim axis binding and the limits of the
  directional contrast.
- `fig-dram-bank-conflict`: SCALE-Sim versus Ramulator DRAM bank conflict latency overhead across access strides.

Executable Quarto figures in the chapter:

- `fig-ai-study-result`: matched cycle-count results for the retained array
  study.
- `fig-proxy-mismatch`: the frozen score plotted against modeled memory
  traffic, exposing an axis that the score omits.

Executable Quarto figures in the chapter:

- `fig-ai-study-result`: matched cycle-count results for the retained array
  study.
- `fig-proxy-mismatch`: the frozen score plotted against modeled memory
  traffic, exposing an axis that the score omits.
