---
format:
  html:
    output-file: index.html
---

# The AI-Native Design Life Cycle {#sec-architecture-20-ontology}

![](images/ch03-cover-map.svg){fig-alt="Six life-cycle responsibilities run from formulation through review and decision, with targeted repair returning to the responsibility that owns an inadequacy."}

::: {.epigraph}
> "All models are wrong but some are useful."
>
> — George E. P. Box, *Robustness in the Strategy of Scientific Model Building* (1979) [@Box1979Robustness, p. 202]
:::

An AI-native design system must connect the generation of alternatives to the work needed to compare, explain, and revise them. This chapter organizes that work into six stages: Formulate, Explore, Implement, Evaluate, Interpret, and Commit. Each answers a different engineering question, from what the design should achieve to what the results justify doing next. Together they provide a working framework for the rest of the book.

The stages describe recurring design activities that may overlap or repeat. Their purpose is to keep the architecture question, the developing candidates, and the results connected as people, models, and tools contribute to a study. A hypothetical block-design example shows how a proposal becomes testable and how its evaluation can guide another design. A separate prospective Lighthouse example illustrates the information needed to review a study. These examples explain the organization; they do not demonstrate a completed AI-native system. Later chapters develop the representations, methods, execution environments, and feedback mechanisms used across the stages.

## The Six Stages of the AI-Native Life Cycle {#sec-arch2-ontology}

The pressures in @sec-design-loop-no-longer-scales affect the whole path from a design idea to an engineering decision. Generating alternatives more quickly helps only if the team can implement them, compare them under meaningful conditions, and use the results to decide what to try next. The life cycle connects those activities so that progress in one can inform the others.

Separating requirements, design alternatives, implementation, and verification has precedents in systems engineering, where these activities are iterative and interdependent [@NASA2017SystemsEngineeringHandbook, chap. 4]. The six stages used here are the book's organization of recurring architecture work. Their usefulness rests on whether they help a reader follow a design, identify what the results establish, and decide what needs revision. They need not correspond to six teams, six software components, or six consecutive steps in every tool flow.

Consider a candidate that misses its clock-frequency target. The target may be unrealistic, the microarchitecture may need revision, or the analysis may have used the wrong constraints. These explanations call for different next steps. A stage boundary helps expose what was assumed, what ran, and what was learned; it does not diagnose the failure by itself. Existing milestones such as register-transfer level (RTL) freeze and timing signoff remain in place. The stages describe work that can recur before and between those milestones.

::: {.callout-design-principle title="Isolate Failure Domains for Repair"}
**The principle:** Separate design activities so that a failed result can be traced to the assumptions, artifacts, or checks that need revision.

**The application:** Use the available diagnostics to choose what to revisit, preserving earlier work whose assumptions still hold. When the cause is uncertain or spans several activities, investigate it before choosing a repair.
:::

Each stage answers a question that the next activity depends on. The sequence provides an initial reading order; feedback can send work back to any earlier stage.

1. **Formulate.** What should the design achieve? State the question, baseline, constraints, permitted changes, and checks that will judge the result.
2. **Explore.** What alternatives are worth testing? Generate or select candidates, including different hardware and software organizations when the question permits them.
3. **Implement.** How can each candidate be tested? Develop the required models, RTL, and software, and run the tools needed to examine them.
4. **Evaluate.** What do the results establish? Check that the observations are valid for the intended comparison, then determine which requirements are satisfied or still unresolved.
5. **Interpret.** Why did the design behave this way? Test explanations for gains and failures, and use what is learned to guide further design work.
6. **Commit.** What should happen next? Review the study and decide whether to advance a candidate, reject it, revise the study, or stop.

The AI-assisted, AI-driven, and AI-native workflows introduced in @sec-moonshot differ in how AI contributes to this work. An assistant might draft RTL during Implement. An AI-driven optimizer might vary pipeline depth and buffer capacity within an established design. An AI-native system would also generate alternative organizations, develop their hardware and software, and use evaluation to guide further redesign. Prediction, generation, and optimization can contribute across the stages; they are methods used within the workflow, rather than additional stages.

The walkthrough follows a hypothetical accumulator block in a neural processing unit (NPU). It asks how to improve throughput while meeting timing and power constraints in a 3\ nm design flow. The candidates, timing failure, and diagnosis are constructed teaching examples. Following one block through the six stages makes their connections concrete (@fig-ontology-loop).

![**The six-stage design life cycle carries system intent to a reviewed decision.** The arrows trace an initial pass through Formulate, Explore, Implement, Evaluate, Interpret (Interpret and Explain), and Commit (Review and Decide). The return arrow illustrates a review that reopens formulation. Other iterations can revisit implementation, evaluation, or exploration without restarting the whole study.](images/fig-ontology-loop){#fig-ontology-loop width="80%" fig-alt="Six stages arranged in two rows, with arrows from Formulate through Explore, Implement, Evaluate, Interpret and Explain, to Review and Decide. One return arrow leads from the decision back to formulation."}

One application of the life cycle to a bounded architecture question is a *scoped study*. The NPU block study is one such application; it does not settle every question about the processor that contains it. Within the study, a design loop occurs when a result changes the design or the plan and leads to another action, as defined in @sec-design-loop-no-longer-scales. Merely completing a predetermined sweep does not supply that feedback.

Studies can also depend on one another. A subsystem study may set a block's power allowance, while the block study may discover that the allowance cannot support a promising design. That finding can prompt a broader reconsideration. The walkthrough first follows one study; the open questions return to the difficulties of coordinating several at once.

### Stage 1: Formulating Core Architecture Questions

Formulation turns a product goal into a question that an architecture study can answer. For the NPU block, "improve throughput" becomes a comparison with the existing accumulator on a specified matrix-multiplication workload. The study asks whether a different organization can process more results per unit time while meeting the target clock period, the block's allocated power and area budgets, and its required numerical behavior. These requirements give later measurements a purpose.

The baseline must be understood well enough to make that comparison meaningful. If it models memory service as instantaneous, for example, an apparent accumulator bottleneck may disappear when realistic contention is included. Formulation therefore identifies the reference design, workload phases, tool assumptions, and checks needed to trust the comparison. It also distinguishes requirements inherited from the surrounding system from choices the study may reconsider.

For this example, permitted changes include the accumulator organization, pipeline structure, and supporting software schedule; the block's external interface and required behavior remain fixed. This leaves room to generate a different design without silently changing the problem. The plan also sets a run budget and stopping conditions, including when to request review because a required change falls outside the agreed scope. The result of formulation is a testable question and a plan for answering it. Exploration can now propose alternatives against that plan.

### Stage 2: Exploring Alternatives

With the question and permitted changes established, exploration develops alternatives worth implementing. Some may tune the existing accumulator by changing pipeline depth or buffer partitions. Others may change its organization. A centralized accumulator connected through a wide crossbar, for example, can be compared with distributed accumulator banks that combine partial results through local connections. A generated proposal for the latter must also describe the control and software scheduling needed to deliver the required results.

These alternatives trade more than throughput. Additional parallel paths may require more storage and routing, while banking may reduce some long connections but introduce conflicts or extra reduction steps. The point of exploration is to expose these tradeoffs and choose informative candidates, not to certify physical feasibility from a high-level description. A simulator that omits wire delay could favor a wider crossbar for reasons that disappear in physical implementation. Predictions from such a model can help screen candidates only within the properties it represents.

The exploration history records why each candidate was selected or set aside. A proposal that violates a fixed interface can be rejected early; one whose timing is uncertain may deserve implementation because it tests a useful design hypothesis. Changes already permitted by the plan can proceed within the study. A proposal that requires a different system interface or power allocation returns to Formulate for review of the scope. The selected alternatives then move to Implement, where their hardware and software descriptions become testable.

### Stage 3: Implementing Testable Representations

A candidate organization needs a testable implementation before its proposed benefits can be assessed. For the NPU, this may involve a transaction-level model (TLM) of data movement, synthesizable RTL for the accumulator and its control, and a compiled software schedule that exercises the design. Implementation develops these artifacts together. A banked accumulator is not a complete candidate if its software still assumes the centralized organization's access pattern.

For a timing comparison, the RTL must pass through the relevant synthesis and physical-design steps. An integrated flow such as OpenROAD Flow Scripts connects logic synthesis, placement, routing, and timing analysis [@OpenROADDocs]. The representation needed depends on the question: an early behavioral model can test an algorithm or schedule, while a physical timing estimate needs the appropriate implementation and electrical models.

Implementation also determines which checks to run first. The constructed example in @fig-verification-wall illustrates how checking costs can differ as the amount of changed code grows. It motivates screening with inexpensive checks before spending more on physical implementation; its assumed times cannot predict the runtime of a particular design flow.

![**Different checking costs motivate staged implementation.** The points are assumed runtimes for linting, RTL simulation, and physical place-and-route over illustrative code changes. They preserve the example's ordering of costs without asserting a complexity law or a relationship that can predict runtime from lines of code. Actual cost depends on the design, checks, algorithms, and resources.](images/plot_ch3){#fig-verification-wall width="80%" fig-alt="Three sets of assumed execution times plotted against changed lines of code on logarithmic axes. In this constructed example, linting takes the least time, RTL simulation takes longer, and physical place-and-route takes the most time. The plot is explicitly labeled as illustrative, not measured."}

An early check saves later work only when it can detect a reason to reject or repair the candidate. Passing that check leaves properties outside its scope unresolved. A behavioral simulation, for example, cannot establish that an implementation will meet the foundry's physical design rules.[^fn-drc-c03] Screening determines which candidates receive further implementation effort; it does not replace the checks needed for the final comparison.

Implementation also checks that the tools examined the intended candidate. A syntactically valid configuration can select the wrong top-level module, hold an active-low reset asserted, or skip the target workload phase. A generated block may pass isolated tests yet violate an interconnect handshake, omit required power-domain isolation, or depend on the wrong compiler target. The candidate, supporting software, interface checks, and tool inputs must therefore remain linked throughout execution.

A failed run needs diagnosis before it can inform a design choice. A license timeout supplies no timing result. An out-of-memory failure may reflect resource allocation, a tool problem, or a candidate whose implementation is unusually demanding. Recording what ran and what failed preserves these distinctions. Evaluate then asks what the completed checks can establish about the design.

### Stage 4: Evaluating Candidates

Once implementation confirms what executed, evaluation checks whether the returned results answer the planned question. Clean execution alone does not establish measurement validity. A power report produced for the wrong workload phase, for example, cannot establish power for the phase being compared. The candidate and baseline must be assessed under comparable conditions, with their required behavior checked before a performance gain is accepted. Several tools may be needed because no single model observes every relevant property.

Suppose a candidate with a wider centralized crossbar compiles cleanly and a C++ performance model predicts higher accumulator throughput at the target clock frequency. In the same hypothetical example, static timing analysis (STA)[^fn-sta-c03] reports a 150\ ps setup timing violation on the accumulator paths, while power-integrity analysis reports voltage drop ($IR$-drop) near critical buffers.[^fn-ir-drop-c03] Evaluation records that timing fails the declared constraint. The throughput prediction assumes a frequency that this implementation has not shown it can sustain, so the candidate cannot yet be accepted as an improvement.

[^fn-sta-c03]: **Static timing analysis (STA)**: Timing analysis that checks circuit paths against constraints without applying simulation input vectors. Its conclusions depend on the timing models, constraints, and paths included in the analysis.

[^fn-ir-drop-c03]: **IR drop**: A voltage decrease caused by current flowing through resistance in the power-distribution network. Reduced supply voltage can slow switching and contribute to timing violations.

Each requirement receives its own outcome: passed, failed, or unresolved. A passing functional check does not cancel the timing failure, and a missing power estimate remains unknown. The voltage-drop observation may help explain the failure, but it does not establish its cause. That is the question for Interpret.

### Stage 5: Interpreting Empirical Results

A qualified passing comparison establishes that the observed quantities satisfied the declared bounds. Both gains and failures still need interpretation, because the same top-line change can arise from different mechanisms. Stage 5 asks which explanation the evidence supports and what observation would distinguish the remaining possibilities.

For the NPU, the architect must distinguish a long crossbar path from inadequate power delivery or an incorrectly configured analysis. An automated system could help by extracting timing paths, proposing explanations with a large language model (LLM), and requesting comparisons that vary a suspected cause. Testing a shorter interconnect path under the same analysis settings, for example, could help assess the contribution of wire delay. Any accompanying changes in buffering, placement, or load must also be examined before attributing the improvement to one cause. Such a test requires another Implement execution and Evaluate comparison before Interpret can use the result.

If the evidence points to long shared connections as the limiting factor, exploration can revisit the distributed-bank alternative. The next iteration would develop its local reduction paths, control, and software schedule, then check timing, power, and numerical behavior again. Interpretation thus helps generate the next design; it does more than attach an explanation to the previous result.

Automating this diagnosis remains a capability to demonstrate within the study's scope. A plausible generated explanation is a hypothesis until observations can distinguish it from alternatives. When they cannot, the study retains the competing explanations and either requests further investigation or stops for review. A useful outcome may be a supported design recommendation, but it may also be a clearer account of what remains unknown.

### Stage 6: Reviewing and Committing Handoffs

Commit brings the study to a reviewed decision. The decision may be to advance a candidate, reject it, revise the question, or stop with unresolved issues. Here, "commit" means deciding what the completed work justifies doing next. The consequence depends on the study: advancing an NPU block to integration is different from approving a design for fabrication.

If a revised NPU block satisfies the declared checks, the responsible architect reviews whether it improves on the baseline and what remains to be checked in the larger system. A recommendation includes the tested design and software revisions, the comparison conditions, the explanation for the improvement, and remaining limitations. Passing this block study does not by itself authorize mask freeze. If timing still fails, the architect can choose further work or close the study without selecting a candidate.

Routine iterations can proceed automatically within the scope and budget established during Formulate. Review is needed when the planned decision is reached, the scope must change, or the stopping conditions call for it. This allows useful automation without treating each tool invocation as a separate approval event. Exhausting the run budget ends the permitted work; it does not establish that no feasible design exists.

The six stages can now serve as a reference for planning a study. Their inputs, outputs, and reasons to revisit earlier work differ, even when one person or tool performs several activities (@tbl-stage-seams).

| **Stage** | **Starts from** | **Produces** | **Reason to revisit** |
| :--- | :--- | :--- | :--- |
| **Formulate** | Product goals and system requirements. | A question, baseline, scope, checks, and stopping conditions. | Evidence calls the target, assumptions, or permitted changes into question. |
| **Explore** | The plan and what earlier candidates taught. | Candidate organizations or configurations, with reasons for testing them. | A candidate fails, or a result suggests a better alternative. |
| **Implement** | A candidate and the checks it needs. | Testable hardware and software, tool inputs, and execution results. | Artifacts need revision, or the intended checks did not run. |
| **Evaluate** | Execution results and the planned comparison. | Valid comparisons and passed, failed, or unresolved checks. | Conditions are mismatched or observations are insufficient. |
| **Interpret** | Checked results and possible explanations. | Supported explanations, remaining hypotheses, and proposed investigations. | Causes remain uncertain or another result contradicts the explanation. |
| **Commit** | The plan, comparisons, explanations, and limitations. | A reviewed decision to advance, reject, revise, or stop. | Missing evidence or a changed requirement reopens the decision. |
: **Each stage contributes something different to a design decision.** The reference distinguishes what an activity needs, what it contributes, and why it may be revisited. A single failure can require work in several stages. {#tbl-stage-seams tbl-colwidths="[15,25,30,30]"}

Evaluate needs both the results from Implement and the criteria declared in Formulate; Commit needs the accumulated work, not only the last interpretation. Repair can also cross several boundaries. A timing violation may send a candidate back to Explore, while evidence that the target itself is infeasible may send the question back to Formulate. Preserving these connections lets another architect follow what happened and why.

## Design-Loop Cards and Documentation {#sec-design-loop-card}

Reviewing the NPU study requires more than its best timing report. Another architect needs to know which candidate produced it, which workload and constraints were used, why that candidate was selected, and what earlier failures taught the team. Much of this information may already exist in repositories, experiment logs, and verification reports. The documentation task is to connect it to the question and the resulting decision.

The book calls this connected account a *study record*. It includes the plan, candidate revisions, comparisons, explanations, failed or unfinished work, and the decision or stopping reason. An *execution record* supplies the details of a particular attempt: its inputs, tool environment, outputs, and status. The distinction matters in the NPU example. One execution record may show a timing violation; the study record explains how that observation led to another accumulator design. A retry retains the failed attempt and identifies what changed, rather than overwriting its directory and losing the reason for the new run.

Logs should retain the inputs and settings that affect a run, including random seeds when applicable, library versions, and diagnostic output. A shared scratch directory is insufficient if later attempts overwrite the information needed to explain an earlier result.

A *Design-Loop Card* is an optional compact index to this information. It borrows the idea of concise, contextual documentation from Model Cards, which describe model uses and evaluation conditions, and Datasheets for Datasets, which document dataset composition and collection [@MitchellEtAl2019ModelCards; @GebruEtAl2021Datasheets]. Here the subject is an architecture study. The card can link existing engineering records; it need not duplicate them or introduce a new project-wide documentation system.

Keeping the study question between the product goal and individual tool runs helps a reviewer see how a local result bears on a larger decision (@fig-structured-layer-above-rtl). The hierarchy describes relationships among information, rather than requiring a particular software architecture.

![**Scoped studies connect system goals to implementation results.** The diagram places the architecture question, constraints, linked artifacts, and review between system intent and the simulators, compilers, and electronic design automation (EDA) tools that examine candidates. The middle layer keeps those results connected to the decision they are meant to inform.](images/fig-structured-layer-above-rtl){#fig-structured-layer-above-rtl width="80%" fig-alt="System intent and human judgment appear above a scoped architecture study, which links its question and artifacts to results from implementation tools below. Feedback connects the study to further design work and review."}

A cycle count, for example, needs its workload and simulator configuration. A physical timing estimate also needs the applicable process, voltage, and temperature (PVT) conditions,[^fn-pvt-c03] library models, and timing constraints. Synopsys Design Constraints (SDC) files provide one way to record the latter.[^fn-sdc-c03] Retaining these links lets a reviewer distinguish a simulated performance improvement from a design that has also met its physical checks. It also reveals which comparisons must be repeated if an assumption changes.

[^fn-pvt-c03]: **Process, voltage, and temperature (PVT) corners**: Combinations of device-model and operating assumptions used to check a design under different conditions. The limiting combination can differ across timing and power checks.

[^fn-sdc-c03]: **Synopsys Design Constraints (SDC)**: A format for specifying clocks, input and output delays, timing exceptions, and related constraints used by synthesis and timing-analysis tools.

::: {.callout-lighthouse title="The Lighthouse XR cache study"}
**Context.** The prospective Lighthouse cache study introduced in @sec-moonshot provides a separate example of how to document a planned comparison.

**Application.** The plan compares 2, 3, and 4\ MiB L2 capacities on the declared XRBench workload, asking whether fewer capacity misses can reduce frame-time tail latency while meeting power, area, and access-time constraints. Power estimates must include leakage within the 3\ W subsystem envelope.

**Outcome.** The study remains undispatched. The plan specifies the evidence needed to consider a cache change; it supplies no measured result.
:::

The Lighthouse example begins with a plan, before any results exist. Its card names the baseline, alternatives, checks, and intended decision (@fig-filled-design-loop-card). Direct enumeration is sufficient to compare the three capacities; this limited study does not require a learned method or demonstrate AI-native redesign by itself. It illustrates documentation that can also support more extensive generated alternatives.

![**A study card separates planned work from results and decisions.** This optional card indexes the prospective Lighthouse cache comparison. Its four-execution entry is a budget, and the results and decision remain open until the study has been run and reviewed.](images/fig-filled-design-loop-card){#fig-filled-design-loop-card width="80%" fig-alt="Illustrative card for a prospective XR cache study. A hypothesis precedes entries for the baseline, candidates, checks, execution budget, evidence needed, and responsible reviewer. No completed result is reported."}

The compact YAML example in @lst-ch03-design-loop-card shows how the same plan could be stored in fields that tools can read. Capacities use MiB consistently, and the objective is the 99th-percentile frame time. The power envelope is a constraint, not a promised reduction. Missing workload details, area and access-time limits, and the reviewer must be supplied before execution; empty result fields remain empty until the relevant checks return usable observations.

```{.yaml #lst-ch03-design-loop-card lst-cap="**A planned comparison leaves its results open:** Compact YAML fields for the prospective Lighthouse cache study."}
# Prospective teaching example; all evidence remains pending.
study_id: "lh_cache_capacity_01"
plan:
  objective: "reduce 99th-percentile XR frame time"
  hypothesis: "fewer capacity misses reduce tail latency"
  baseline_l2_mib: 2
  candidate_l2_mib: [2, 3, 4]
  other_architecture_choices: "fixed"
  method: "direct enumeration"
  constraints:
    subsystem_power_limit_w: 3 # Includes leakage.
    area_limit_mm2: null # Set before execution.
    cache_access_time_limit_ns: null
    required_checks: [functional, power, area, timing, tail_latency]
  budget:
    cycle_level_executions_including_reruns: 4
    sram_evaluations_per_capacity: 1
execution:
  status: "NOT_DISPATCHED"
  workload_trace: null
  software_image: null
  tool_and_model_versions: null
  records: []
results:
  status: "PENDING"
  comparisons: [] # Include uncertainty and links to raw outputs.
  failed_or_unfinished_runs: []
  explanation: null
review:
  reviewer: null # Assign before execution.
  decision: "PENDING"
  permitted_next_step: "advance at most one candidate to RTL evaluation"
```


Four cycle-level executions allow the plan to examine three capacities with limited room for a rerun. That budget does not guarantee a conclusive tail-latency comparison. If too few valid observations remain, or the measured differences cannot be resolved with the available evidence, the result stays unresolved. The NPU walkthrough illustrates how a study might reason through a failure; it supplies no evidence for this separate cache comparison.


The following technical chapters develop the ingredients used throughout this life cycle. @sec-data-representations-world-models examines how design information is represented; @sec-methods-generation-prediction-optimization develops prediction, generation, and optimization; @sec-architecture-environments-tool-interfaces explains reliable tool execution; and @sec-feedback-verification-trust develops verification and feedback. These ingredients serve several stages, rather than matching the stages one for one. @sec-running-the-loop then brings them together in a worked application.

## Open Questions

Separating architectural design into six responsibilities makes local failures easier to investigate. The harder cases arise when their causes cross the boundaries, or when the information needed for repair has disappeared. These limits leave several questions for deploying the life cycle in practice.

- **How does an AI-native design system isolate a failure that no single stage owns?** Targeted routing works most directly when the defect and the responsibility for repair can be identified. Real flows already sort the easy cases, since tools carry message identifiers, violation reports name an endpoint and a rule, and schedulers separate an out-of-memory kill from a design fault. The hard case is the one that was legal everywhere it passed, and a timing violation at signoff is jointly a property of the register-transfer description, the constraints, the floorplan, the library view, and the clock tree. Attribution may not be possible for that class at all. Whether automated systems can apportion blame across coupled stages through sensitivity analysis, or must investigate coordinated changes across several responsibilities, remains an open limit of targeted repair.

- **Does dividing the work create the failures it was meant to isolate?** Historically, hierarchical closure in physical design presented similar integration challenges. Hierarchical closure with block budgets was introduced so blocks could close independently, and it produced interface timing thrash, budget churn, and the return of full-flat signoff, because voltage drop and crosstalk do not respect a boundary somebody drew. Partitioning is offered here as what keeps automated output inspectable, and the cost of drawing the boundaries is never set against that. Nobody has counted seam-induced integration failures separately from intra-stage bugs on a real design flow, leaving open whether clean stage boundaries accelerate overall closure or merely invent new places for designs to stall.

- **How does a design loop discover that the question was wrong while tools keep answering it?** A budget can stop a study without establishing infeasibility; the harder question is whether returned evidence can justify revising the target itself. A synthesis tool may keep reporting the best timing it achieved without establishing whether the requested frequency is reachable in the given library. Repeated attempts may return the same critical path, but near misses alone do not prove infeasibility. An analytical bound could establish that a target is unattainable under declared assumptions without exhausting the design space. The open problem is determining when such a bound, together with tool returns, provides enough evidence to reopen the target rather than continue searching or stop unresolved.

- **What artifacts must be preserved so an engineering team can reconstruct a decision five years later?** The study record is offered here as what makes a decision reviewable after the fact, and its durability is assumed rather than shown. A respin, a process revision, or a field failure all force a team to reconstruct why a choice was made. The process design kit (PDK) cannot leave the foundry, the tool version may not be licensable by then, and the model that made the call may have been retrained or thrown away, so the inputs are exactly the things that cannot be kept. Preserving raw inputs is one answer that quickly decays. Preserving the explicit claims and empirical evidence bounds is another. Whether durable reproducibility requires storing full execution containers, or can rely on cryptographic attestations that certify constraints were checked without preserving proprietary tool environments, remains an open choice.

- **What synchronizes hundreds of concurrent studies when underlying dependencies shift?** Regression managers already run tens of thousands of jobs a night, and they hold together runs that share one baseline. The arrangement described here is nested instead, with subsystem studies spawning microarchitecture studies spawning implementation studies, while the contract on offer is for one loop running alone. Hundreds of separate studies each pin their own tool versions and workloads, run for weeks, and a library recharacterization or compiler update moves beneath all of them at once. Whether dependency graphs can track baseline invalidation across concurrent studies automatically, or teams must continually reexecute past explorations to purge stale conclusions, remains an unsolved systems challenge.

## Summary

The life cycle's claim is that stage boundaries exist for repair, not for ceremony. Work that fails independently is owned separately, so an inadequate result returns to the responsibility that can fix it with the question, the candidates, the tool state, and the valid prior results intact. What a study may touch, what it compares against, and when it stops are declared before the first candidate is proposed, and the study record keeps every attempt, including the failed and unevaluated ones, inspectable by an architect who was not present.

::: {.callout-takeaways}
- **Stage contracts route repair.** Each stage receives declared inputs and produces a checked output, so an inadequate result returns to the stage that owns its repair rather than restarting the study or corrupting shared state.
- **Declared stopping conditions.** A scoped plan binds its stopping conditions before the first candidate is proposed. A supported recommendation, a rejected slate, or an unresolved finding can each end the planned work; exhaustion of the budget does not establish infeasibility.
- **Empirical mechanism explanation.** Architectural interpretation must connect top-line metric changes to concrete microarchitectural mechanisms, isolating hardware gains from simulator artifacts or benchmark loopholes.
- **Audit-ready study records.** Every consequential design transition requires a version-controlled study record that documents baseline assumptions, candidate variations, tool outputs, failed runs, and named commitment authority.
:::

An operational workflow is only as good as the representations of architectural state that its generators, optimizers, and tools can act on, and how that state is formalized, discovered, and maintained across the design stack is the subject of @sec-data-representations-world-models, which opens Part II.


[^fn-drc-c03]: **Design rule check (DRC)**: A check of layout geometry against manufacturing rules, such as minimum wire widths and spacing. Passing behavioral simulation does not establish compliance with these rules.

[^fn-rc-delay-c03]: **RC Delay and Routing Thickets**: Resistance ($R$) and capacitance ($C$) delay signals traveling through wires. If an optimizer ignores these physical limits, it might aggressively pack too many components (like buffer arrays) into the floorplan, creating a congested "thicket" where wires can no longer be legally routed.

[^fn-rtl-c03]: **register-transfer level (RTL)**: A design abstraction that models a synchronous digital circuit in terms of the flow of digital signals (data) between hardware registers, and the logical operations performed on those signals.

[^fn-tlm-c03]: **transaction-level models (TLMs)**: High-level C++ or SystemC models that simulate communication between modules as function calls (transactions) rather than cycle-by-cycle pin toggles, allowing for much faster software development and early architecture exploration.

[^fn-crosstalk-c03]: **Crosstalk**: Signal interference caused by capacitive coupling between adjacent wires on a chip, which can degrade timing and corrupt data.

[^fn-pdk-c03]: **process design kit (PDK)**: A set of foundry-specific files and models that describe the manufacturing process (like TSMC 3nm), providing the physical rules and electrical characteristics required for EDA tools to design the chip.

[^fn-tdp-c03]: **thermal design power (TDP)**: The maximum amount of heat generated by a computer chip or component that the cooling system is designed to dissipate under typical workloads.
