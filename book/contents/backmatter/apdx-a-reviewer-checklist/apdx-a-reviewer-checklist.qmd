---
format:
  html:
    output-file: index.html
---

# The Architecture 2.0 Reviewer's Checklist {#sec-appendix-a-reviewer-checklist}

A reviewer judging a paper, an artifact package, or a design report on AI for computer architecture needs to establish that the reported advance is progress in the architecture rather than an artifact of the comparison. The order of inspection matters. Problem formulation and baseline fairness come first, because a flaw there can undermine the conclusions drawn from downstream measurements; representations, data hygiene, proxy fidelity, and execution governance follow.

Hardware raises the stakes of that inspection because it has physical and formal ground truth. A logic flaw in silicon can require a mask respin and consume a market window; a software patch is not always an available remedy. A model that reaches low loss on an abstract dataset can still fail when its output meets timing, power, and design rules. At the same time, the field spans many abstraction levels, and the evidence one paper owes differs from the evidence another owes. Demanding tapeout signoff from a microarchitectural policy paper is unreasonable; accepting unsynthesized, unlinted register-transfer level (RTL) from a paper claiming implementation-ready RTL leaves physical realizability unestablished. The checklist handles that range through its recording vocabulary rather than through separate rubrics: an item that a paper's claim does not reach is recorded as not applicable, and a reviewer should expect to record several.

This checklist serves three readers with one text. A reviewer applies it item by item. An author reads it before submitting and learns what the work will be asked for. A student reads it and learns what the field treats as evidence and why. The why is carried at the item level: every check states the question, the failure mechanism that makes the question necessary, and what defensible evidence looks like, and each closes with a one-line statement of what the item catches. None of the items requires the vocabulary or the record formats this book introduces; a program committee can adopt the checklist without adopting the book.

The checklist has five areas, ordered from system framing down to operational governance. Chapter pointers in this list are for readers of this book and are not part of any item.

1. **Area 1: System Framing, Problem Scope, and Method Justification** (is learning warranted; matched baselines; @sec-from-architecture-10-to-architecture-20)
2. **Area 2: Data Representations and Physical Grounding** (hardware semantics, structural faithfulness, legal decodability; @sec-data-representations-world-models)
3. **Area 3: Data Integrity, Leakage, and Generalization Boundaries** (benchmark decontamination, temporal and feature leakage, out-of-distribution transfer; @sec-data-representations-world-models; @sec-evaluating-agentic-architect)
4. **Area 4: Learning Methods and Proxy Fidelity** (validation of generated artifacts at the level the claim requires, tail-ranking correlation, optimizer anti-gaming, multi-objective constraint integrity; @sec-methods-generation-prediction-optimization)
5. **Area 5: System Orchestration, Reproducibility, and Ownership** (lineage, replayable environments, gross cost accounting, human commitment authority; @sec-running-the-loop; @sec-evaluating-agentic-architect; @sec-what-architect-owns)

## Recording Findings

For each applicable item, record what evidence the work supplies, where it can be found, and what it establishes or leaves open. Findings describe the evidence; they do not grade the work. The following labels can help, but plain-language notes or an existing review format serve the same purpose.

- **Supported.** The work supplies the evidence the item asks for, and the reviewer can point to where.
- **Conditional.** The evidence satisfies the check only within a stated restriction, such as a single technology node or a single benchmark family. The reviewer records that restriction and whether the claim stays within it. A single seed, for example, documents one run but leaves variability across seeds unresolved; it does not by itself support a broader reliability claim.
- **Unresolved.** The available evidence does not establish whether the failure the item guards against has occurred. Record what is missing and what the supplied evidence does establish. Absent information is not evidence of failure. If the work instead demonstrates a failure, describe it directly and state which part of the claim it undermines.
- **Not applicable.** The claim does not reach the item. A microarchitectural policy paper whose claims are limited to cycle-level simulation does not owe post-layout signoff; a paper on learned cost models does not owe legalization results unless its claims depend on producing legal designs. Record why the item falls outside the claim.

The findings do not add up to a score. There is no count of supported items that constitutes acceptance, no count of unresolved items that constitutes rejection, and no weighting. For each restriction or gap, state how it limits the claim and what an unfavorable result would undermine. Form the recommendation separately. A submission can supply the evidence for every applicable item and still make a claim that evidence does not reach; a submission with several restrictions can still be worth publishing with its claim narrowed.

These item-level findings differ from the claim judgments in @sec-evaluating-agentic-architect. That chapter assesses whether a program claim is supported, unsupported, or unresolved through matched comparisons and declared decision boundaries. Here, the reviewer records what individual pieces of evidence establish. Every applicable checklist item may be satisfied while the program claim remains unresolved because the comparison lacks the resolving power to decide it. When using both sets of terms, specify whether a finding concerns an individual check or the overall claim.

## Area 1: System Framing, Problem Scope, and Method Justification

Before assessing model architectures, loss functions, or training curves, a reviewer examines whether the problem formulation is sound and what the proposed method offers over relevant alternatives. Area 1 examines method suitability, the fairness of comparisons, the contribution attributable to the work, and the net effect on engineering time.

- **Check 1.1: The Method Justification and Overkill Test.** Assess whether simpler analytical formulations, dynamic programming, integer linear programming, or greedy heuristics provide suitable alternatives under the problem's constraints. Compare solution quality, runtime, and any guarantees relevant to the claim. Learning can introduce training overhead, inference latency, nondeterminism, and difficulty interpreting results. If a problem has exploitable structure (a bound such as Roofline [@Williams2009Roofline], optimal substructure suited to dynamic programming, or a scheduling rule suited to a greedy heuristic), the evaluation should explain what remains unresolved and what learning contributes. A bound, for example, need not provide a method for constructing a design that attains it. High-dimensional nonlinear interactions, layout parasitics, and multi-objective tradeoffs can motivate a learned approach, but their presence alone does not establish its advantage. Evidence should compare relevant methods on the stated task and account for variability [@AgarwalEtAl2021StatisticalPrecipice]; it need not prove that every classical alternative fails. **Catches:** added method complexity whose benefit over a suitable simpler approach has not been established.

- **Check 1.2: Matched Compute Budgets and Baseline Parity.** Verify that learned search (reinforcement learning, Bayesian optimization, or neural tree search) is compared against credible conventional baselines with documented tuning effort and resource budgets [@AgarwalEtAl2021StatisticalPrecipice; @ChengEtAl2023MacroPlacement]. When a multi-GPU learned optimizer runs $10^5$ design evaluations while the simulated-annealing or genetic baseline is limited to $10^2$ evaluations on one CPU core, the reported margin alone cannot distinguish a method advantage from a resource advantage. Match the budget relevant to the claim: sample limits for evaluation efficiency, wall-clock time for turnaround, or simulator and license invocations when those constrain the work. These budgets need not all be identical simultaneously. Quality-of-results curves against the relevant budgets, with hardware and tuning effort disclosed, help readers see where an advantage holds. Account for training and other preparation costs, and state any assumptions about amortizing them across later searches. **Catches:** a resource advantage presented as evidence of algorithmic superiority.

- **Check 1.3: Methodological Contribution and Model Dependence.** Identify what the work contributes beyond the underlying model and test whether that contribution explains the reported improvement. Contributions may include decoding guided by an abstract syntax tree (AST), neurosymbolic grammar verification, closed-loop compiler repair, a domain representation, a specialized training objective, or a prompt-based method. VeriGen, for example, evaluates a model fine-tuned for Verilog generation [@ThakurEtAl2023VeriGen]. A prompt template around a commercial foundation model requires the same scrutiny: what does it add, under what conditions does it help, and how sensitive is the result to the model and prompt choices? A prompt-centric workflow should compare against simpler prompts and examine relevant variation in temperature, random seed, and prompt wording. Where the claim extends across checkpoints or models, test that scope; otherwise, state the dependence on the evaluated model. Judge prompt choices by their effects on correctness, quality, cost, and repeatability, rather than by stylistic preference. **Catches:** improvement attributed to the proposed method without evidence separating its contribution from the underlying model's capability.

- **Check 1.4: End-to-End Workflow Speedup and Amdahl's Law.** Audit total human engineering effort and elapsed time across the workflow. Amdahl's law explains why accelerating one stage yields a smaller overall speedup when other stages remain unchanged [@Amdahl1967ValiditySingleProcessor; @AltafWood2017LogCA]. Automation can also change the amount of work in those other stages. If a tool drafts an accelerator block in seconds instead of days but introduces timing bugs and protocol glitches that take weeks of additional manual triage and formal verification, project throughput may fall (@sec-data-scarcity-verification-load). The evaluation should report human interaction time, prompt iteration counts, and manual repair interventions alongside generation time, and compare workflows that reach the same required outcome. A net acceleration claim requires lower total time within the stated workflow boundary; downstream verification and debugging may increase if the savings elsewhere outweigh that increase. Report human effort separately from elapsed time so that parallel execution or unattended tool runs do not obscure the tradeoff. **Catches:** a local speedup presented as a workflow improvement without accounting for added work elsewhere.

## Area 2: Data Representations and Physical Grounding

Learned models in architecture operate on representations of circuits, layouts, and traces rather than on silicon. A representation must retain the properties needed to support the study's claim, and those properties must survive the relevant compilation and implementation steps. Area 2 examines structural faithfulness, decodability, legalization, and consistency across abstraction levels. The required checks depend on what the study claims: not every representation must encode every physical detail, and not every study requires physical signoff.

- **Check 2.1: Hardware Semantic and Structural Faithfulness.** Verify that the representation preserves the properties relevant to the claim, such as circuit hierarchy, multi-clock domains, bitwidths, control and data flow, and spatial adjacency. Circuits are spatial, concurrent, and hierarchical [@BachrachEtAl2012Chisel]; inspect whether flattening or other transformations discard information needed for the task. A flat token sequence is not by itself evidence of semantic loss: the question is what the sequence encodes and what the model or downstream tools can recover (@sec-hardware-representation-formats). For a claim about wire congestion, layout parasitics, or modularity, the evaluation should identify the required spatial or structural information and show how the workflow uses it. For generated designs, include decoded examples and checks that interfaces, widths, and relevant constraints survive conversion into the form consumed by downstream tools. **Catches:** a representation that cannot express the property the claim is about.

- **Check 2.2: Legalization Robustness and Post-Legalization Quality Delta.** When a continuous generative model (a diffusion model or a variational autoencoder) proposes floorplans or macro placements, examine whether automated placement tools can legalize the geometry and report any manual intervention [@MirhoseiniEtAl2021GraphPlacement; @ChengEtAl2026UpdatedAssessment]. Floating-point coordinates may produce overlapping macros, illegal cell densities, or pin blockages that obstruct routing. If legalization substantially displaces the placement, the wirelength and timing predicted for the original proposal may no longer describe the resulting layout. The evaluation should report the automated legalization success rate together with quality-of-results before and after legalization, identifying which quantities are surrogate estimates and which are measured by downstream tools. Legalization alone does not establish design-rule checking (DRC) or layout-versus-schematic (LVS) success. Report those checks separately when the claim extends to that level of physical verification; otherwise, state what remains untested. **Catches:** a placement quality measured before the placement was legal, or legalization presented as physical signoff.

- **Check 2.3: Multi-Fidelity Representation Consistency.** When conclusions drawn from a high-level abstraction (a transaction-level model or an instruction trace) are intended to hold at a lower level, check the timing and resource assumptions on which those conclusions depend. High-level abstractions may omit or simplify resource contention, cache port conflicts, and wire parasitics [@AkramSawalha2019SimulatorSurvey]. An optimizer may favor candidates that benefit from those omissions, so a Pareto frontier in the search model needs validation before it supports a lower-level claim. The evaluation should compare representative candidates, including the selected candidates, at the fidelity needed for the conclusion. Depending on the claim, this could involve cycle-accurate RTL simulation, post-synthesis timing, or further physical evaluation. Report whether candidate rankings and claimed tradeoffs survive, and explain consequential reversals; positive overall correlation alone may conceal an error in the selected candidate. If no lower-level evaluation is available, bound the conclusion to the abstraction tested. **Catches:** a ranking that reverses at the abstraction level needed to support the claim.

## Area 3: Data Integrity, Leakage, and Generalization Boundaries

A reported gain is difficult to interpret if evaluation circuits may have appeared in training, test information enters the feature pipeline, or predictions extend beyond the conditions evaluated. Area 3 checks what the data support: performance on familiar designs, generalization to held-out designs, or transfer to a changed architecture problem. The evidence needed depends on which claim is being made.

- **Check 3.1: Benchmark Decontamination, Zero-Point Baselines, and Harness Isolation.** Examine whether an autoregressive model could have encountered the public benchmark suites it is evaluated on, such as VerilogEval [@VerilogEval] or ChiPBench [@WangEtAl2025ChiPBench], and whether a tool-using evaluation harness isolates state between independent episodes and rejects vacuous passes [@ZhuEtAl2025AgenticBenchmarks]. Residual state that leaks between independent episodes, and reward rubrics that credit an empty response or a reset-only module, can inflate reported capability. The benchmark diagnostics discussed in @sec-evaluating-agentic-architect distinguish exploit scores from contamination indicators; the latter do not establish how much memorization contributed to performance (@fig-benchmark-health-zero-point-contamination). Authors should report what is known about training-data overlap, the contamination checks performed, and what remains unknown. Where pretraining data are undisclosed, absence of detected overlap does not establish absence of contamination. If canary strings are used, explain their role and what their detection or absence establishes. Document which filesystem and memory state is cleared between independent episodes and which resources are intentionally shared. Evaluate a do-nothing baseline (an empty module or no-op generator) to establish the zero-point floor, and test on parameterized mutants that break textbook patterns (a non-power-of-two first-in, first-out (FIFO) depth, an inverted handshake polarity, a custom instruction-set extension). Such variants probe behavior beyond the original examples; they do not by themselves establish that the benchmark is uncontaminated. **Catches:** possible test-set recall, unintended cross-episode information, and credit awarded for doing nothing.

- **Check 3.2: Temporal, Hierarchical, and Feature Leakage.** Check that training, validation, and test partitions match the intended generalization claim. Holding out design blocks tests a different boundary from holding out system families or later tapeout revisions; a study need not impose all three boundaries unless its claims require them. Computing normalization statistics over the full dataset, or splitting randomly across interconnected submodules, can expose test information during training and undermine a claim of independent evaluation [@BaggerlyCoombes2009; @Balloccu2024Leakage]. In hardware, placement hints or post-synthesis timing from a later tapeout must not be visible to a model predicting an earlier one. The feature pipeline should identify when each input becomes available, which data are used to fit normalization and other preprocessing, and whether subcircuits or related revisions occur on both sides of a split. Explain whether that overlap reflects intended reuse or compromises the claimed holdout boundary. **Catches:** test information entering through preprocessing, related designs, or features unavailable at prediction time.

- **Check 3.3: Out-of-Distribution Validity Envelopes.** Require an explicit statement of the conditions under which the surrogate has been evaluated, including process design kit (PDK), microarchitectural topology, and circuit scale. Migration from planar SkyWater SKY130 at $130\ \text{nm}$ to a FinFET node, or from fixed aspect ratios to a 3D chiplet arrangement, changes the prediction task and calls for new evidence. Distributional shift is a general learned-system risk [@AmodeiEtAl2016Concrete]; that observation alone does not establish whether a particular hardware model will fail. Check which device, interconnect, and geometric assumptions change, and test predictions against relevant target-condition results (@sec-support-limits). Physics-informed constraints, such as RC scaling and IR-drop equations, and uncertainty estimates can help identify implausible or unsupported predictions, but they must themselves be checked under the target conditions. State what remains outside the evaluated range. **Catches:** an extrapolation presented with more confidence than the target-condition evidence supports.

## Area 4: Learning Methods and Proxy Fidelity

With representation and data hygiene established, reviewers can examine how generative synthesizers, predictive surrogates, and closed-loop optimizers support the reported claims. These method families may be used separately or combined. Fast proxies can diverge from the behavior they approximate, and search can favor candidates where that divergence is greatest. Area 4 checks correctness evidence, selected-candidate fidelity, simulator assumptions, and objective and constraint accounting. The required evidence depends on what the study claims to establish.

- **Check 4.1: Generative Synthesis Beyond Compilation and Pass@1.** Audit generated RTL for the functional and stateful behavior required by the claim, beyond compilation alone [@ThakurEtAl2023VeriGen; @VerilogEval]. `Pass@1` measures success for a single generated candidate under the evaluation's pass criterion; its meaning depends on the tests or checks used. A high `Pass@1` on leaf modules (multiplexers, shift registers) therefore does not by itself establish correctness for more complex multicycle behavior. Relevant failure modes include functional vacuity [@BeerEtAl2001Vacuity], clock-domain-crossing metastability, asynchronous reset glitches, and unintended inferred latches. Depending on the design and claim, evidence could include lint results (`verilator --lint-only -Wall`), inspection of inferred storage, clock-domain crossing (CDC) checks, nonvacuous formal proofs with SystemVerilog Assertions (SVA), bounded model checking with stated bounds, and synthesis results. An open PDK and open tools can make implementation checks accessible [@AjayiEtAl2019OpenROAD; @SkyWater2020SKY130PDK]. The report should explain which properties were checked, under which assumptions, and what remains untested; no single tool result establishes every form of correctness. **Catches:** code that passes a limited check while the claim assumes behavior that check does not cover.

- **Check 4.2: Predictive Surrogates, Tail-Ranking Fidelity, and the Optimizer's Curse.** When evaluating a surrogate for design-space exploration, examine its accuracy among the candidates the optimizer selects. Ranking correlation (Kendall's $\tau$ or Spearman's $\rho$) within the top one to five percent of candidates can be useful, provided the report identifies how that subset was selected and what reference evaluations establish its ranking. Aggregate $R^2$ or root-mean-square error over millions of random unviable designs can obscure errors among promising points. Selection based on uncertain estimates can favor candidates with overly favorable errors, the optimizer's curse [@SmithWinkler2006OptimizersCurse] (@sec-law-optimizers-curse). This need not invert the entire ranking to exaggerate the selected candidate's benefit. Evidence could include reference evaluations of selected candidates, their prediction errors and ranking correlation, and uncertainty estimates checked in the region reached by search. If uncertainty guides selection, explain how unsupported extrema are handled. **Catches:** a surrogate that is accurate on average and misleading exactly where the search goes.

- **Check 4.3: Closed-Loop Optimizers, Anti-Gaming, and Simulator Loopholes.** Check whether a search algorithm can exploit inaccuracies or omitted constraints in a fast simulator, such as omitted DRAM refresh, bypassed coherence serialization, or ignored bus wait states. Search can favor candidates that benefit from these abstraction shortcuts [@Strathern1997Goodhart; @AmodeiEtAl2016Concrete]. Evaluate the winning candidate and its comparison baseline with a check that addresses the suspected omission. Depending on the claim, this could involve cycle-accurate RTL simulation in Verilator [@Veripool2026Verilator] or field-programmable gate array (FPGA) emulation in FireSim. Identify which relevant behaviors the validation model includes: changing tools does not help if both omit the same effect. Report whether the gain persists, changes, or remains unverified under that check. **Catches:** a speedup that exists only in the simulator's blind spot.

- **Check 4.4: Multi-Objective Pareto and Constraint Integrity.** Check whether improvement along one objective violates a relevant boundary such as thermal design power (TDP), static random-access memory (SRAM) area budget, or voltage droop. A speedup does not establish a feasible improvement if the candidate exceeds the stated power or die-area budget. The objectives depend on the task: BOOM-Explorer examines power and performance tradeoffs [@BaiEtAl2021BOOMExplorer], whereas Ansor evaluates tensor-program execution performance [@ZhengEtAl2020Ansor]. Neither objective choice makes unmeasured constraints satisfied. The evaluation should distinguish objectives from hard constraints and report the objective vectors supporting its Pareto claims, together with constraint values at matched or explicitly reconciled operating points. Identify violating points separately from the feasible frontier and state how feasibility was assessed. Estimated constraints should remain labeled as estimates; an early exploration need not provide physical signoff unless its claims require that evidence. Record missing constraint evidence separately from an observed violation, leaving the acceptance decision explicit and separate. **Catches:** a gain on the reported axis paid for on an unreported one.

## Area 5: System Orchestration, Reproducibility, and Ownership

An AI-native design system can compose automated agents, deterministic compilers, and electronic design automation (EDA) tools into closed loops. Operating such a system calls for scrutiny of reproducibility, of the full cost of the search, and of who decides. Area 5 examines execution lineage, complete resource accounting, and responsibility for consequential commitments, including committing a design to silicon.

- **Check 5.1: Execution Lineage, Replay, and Environment Identification.** Assess whether the execution record and available artifacts support inspection and rerunning of the claimed result [@ACM2020ArtifactReview]. These workflows depend on heterogeneous toolchains: foundation-model endpoints, Python packages, EDA binaries, synthesis scripts. Record the tool and package versions, exact EDA script flags, random seeds, and available model checkpoint identities and access dates. Where used, include container image digests (`sha256:...`) and build attestations; other environment descriptions may suffice when they identify the dependencies needed to rerun the work. Distinguish exact replay from repeating an experiment to assess whether its conclusion holds. If a checkpoint is a floating alias (`model-latest`), or seeds and flags are undocumented, state which parts cannot be reconstructed. Retained inputs, outputs, tool logs, and independent checks can still support findings about the recorded run, while leaving repeatability unresolved. **Catches:** a result whose execution cannot be traced, or whose reproducibility is claimed beyond what the available artifacts support.

- **Check 5.2: Gross Cost and License Accounting.** Account for the total compute and economic cost of the exploration, including failed runs, timed-out simulations, and the commercial EDA license time consumed during search. Reporting only the compute of the final inference pass conceals the cost of the search that found it. The broader concern is that model code is only one part of a deployed learning system [@SculleyEtAl2015Hidden]. In a hardware workflow, measure the costs of crashed simulations, pruned branches, and license use rather than assuming which dominates. The evaluation should present a cost record that covers every run, not only the successful ones, and that sets token and model costs beside EDA license hours. Include human effort in setup, debugging, intervention, and review, and identify any costs that were estimated or unavailable. **Catches:** the finalist's cost reported as the search's cost.

- **Check 5.3: Automated Actions and Human Decision Authority.** Confirm that the work distinguishes actions delegated to automation from commitments requiring a named human authority (@sec-what-architect-owns). @Bainbridge1983Ironies explains how automation can leave difficult monitoring and intervention tasks to human operators. Naming a reviewer therefore does not, by itself, establish that the person can assess the proposed decision. A workflow may authorize automated proposal, evaluation, and advancement within specified limits without requiring a person to approve every step. The work should state those limits, identify the decisions that require human approval, and show how exceptions reach the responsible person. For consequential commitments such as signoff or committing a design to silicon, present the tradeoffs and residual risk that person is being asked to accept. **Catches:** a system that has quietly acquired authority beyond its assigned limits.

## Required Content of a Complete-Workflow Report

The seventeen checklist items above can be applied individually to work that claims a single component: a learned cost model, a placement policy, or a generator. Work that claims a complete human-plus-technical workflow must also explain how those findings bear on the same tasks, comparisons, and claims. The six parts in @tbl-complete-system-assessment provide a reading order for that explanation. The system configuration and comparator establish what was tested; the architecture result establishes what happened; controlled component comparisons help explain why. A component can make a measurable contribution even when the complete workflow does not improve on the practical alternative. Conversely, an overall improvement does not establish that every component helped. The final statement of claims should preserve these distinctions and the limits of the evidence.

| **Part of the report** | **Required content** |
| --- | --- |
| **System identity and comparator** | The configured system, the complete workflow, the architectural task, the environment, the legal actions, the hardware checks, the budgets, and the strongest practical alternative. State what was fixed before the runs, what changed during exploration, and which configuration produced each reported result. Check 1.2 addresses comparison budgets; explain which resources were matched and how remaining differences affect the comparison. |
| **Architecture result** | For each hardware task, the supported result or the failure, the comparator it was measured against, its uncertainty, the deepest check that completed, and the nonclaims stated outright. Manufacturing, test, packaging, or bring-up evidence appears only when the evaluation reached those stages. Check 4.1 addresses correctness and implementation evidence; this part asks where each task's evidence stops and whether it reaches the level needed for the claim. |
| **Decision behavior and component contribution** | Useful observations, supported decisions, false accepts and false rejects, escalations, stops, recoveries, and human overrides, reported where applicable as counts a reader can audit. State how decision errors were established and identify cases whose correctness remains unknown. Separate the complete-workflow effect from each claimed component contribution through a controlled comparison that removes or replaces that component; where such a comparison is absent, leave the contribution unattributed. |
| **Total cost and candidate flow** | Resource classes, per-run design costs, and the candidate flow of every comparison arm from proposal to acceptance or rejection, with failed and censored work retained and shared costs accounted for without double counting. Check 5.2 already asks for the cost record; this part asks that the flow of candidates through it be visible per arm. |
| **Reliability, benchmark, and challenge results** | Repeated behavior across seeds and reruns where relevant, the envelope of conditions tested, the versions and health of every benchmark used, and red-team and fault-injection results where relevant to the claim. Name untested conditions and structural attacks that limit the conclusions. Checks 3.1 and 4.3 already ask for benchmark health and simulator loopholes; this part connects their coverage and remaining gaps to the workflow's reliability claims. |
| **Claim status** | The complete-workflow claim and each narrower component claim stated separately, with the tasks, conditions, costs, architectural checks, and challenge coverage within which each is asserted, and a plain statement of which of them the evidence supports, which it does not, and which it leaves undecided. |
: **The final report connects the evidence to the claims rather than repeating it.** Six parts in reading order, from the system and comparison through architecture result, decision behavior, cost, and reliability to the claim status. A part that overlaps a checklist item points back to that evidence and explains what it establishes for the workflow. {#tbl-complete-system-assessment tbl-colwidths="[26,74]"}

The last row asks authors to state their claims and the scope of each in their own words. It requires neither a particular record format nor the program-claim verdict defined in @sec-evaluating-agentic-architect. The item findings recorded against Areas 1 through 5 supply evidence for assessing those claims; they are not numerical scores or acceptance recommendations. Missing evidence can leave a claim undecided, while observed failures may contradict it or restrict its scope. Without an explicit statement of claims, even sound item-level evidence leaves the reader to guess what the workflow is meant to have established.

## Findings Summary

@tbl-ai-arch-evaluation-matrix gathers the five areas for a reviewer filling in findings: what each area protects, the evidence a supported status rests on, and the shapes that an unresolved finding most often takes. It is a summary of the items, not a scoring grid; the statuses recorded against the items are the finding, and nothing in the table converts them into a number or a recommendation.

| **Area** | **What it protects** | **Evidence a supported status rests on** | **Common shapes of an unresolved finding** |
| :--- | :--- | :--- | :--- |
| **1: System Framing and Scope** | The problem is worth learning and the comparison is fair | Ablation against classical heuristics and analytical models; matched-budget baselines; end-to-end effort accounting | Learning applied to a closed-form problem; a starved baseline; the Amdahl consequence unexamined |
| **2: Data Representations** | The representation can express the property the claim is about | Structural faithfulness demonstrated; legalization success with bounded post-legalization degradation; cross-fidelity ranking agreement | Flattened hierarchy; unroutable decoded output; a frontier that inverts at synthesis |
| **3: Data Integrity and Leakage** | The gain is reasoning, not recall | Decontamination disclosure; temporal and block-level split isolation; a stated validity envelope | Memorized benchmark suite; normalization computed over the test set; extrapolation outside support |
| **4: Learning Methods and Proxies** | The proxy agrees with physical ground truth where the search goes | Full signoff of generated artifacts (STA, CDC, design rule check (DRC) and layout versus schematic (LVS)); top-$k$ rank correlation; anti-gaming cross-validation; complete Pareto vectors | Compilation-only evaluation; aggregate accuracy only; a gain that vanishes in cycle-accurate simulation; a violated unreported constraint |
| **5: System Orchestration** | The result can be replayed, its cost is known, and a person owns the decision | Pinned image digests and checkpoints; gross cost record covering failed runs; an explicit statement of automated versus human action rights | Floating model endpoints; finalist-only cost; an automated system with commitment authority |
: **Findings summary for the five checklist areas.** Each row names what the area protects, the evidence a supported status rests on, and the common shapes of an unresolved finding. The table summarizes the items; it does not score them. {#tbl-ai-arch-evaluation-matrix tbl-colwidths="[18,24,30,28]"}

Applied in order, the checklist separates architectural progress from compute-scale artifacts, benchmark recall, and uncalibrated proxies, and it leaves the reviewer with findings that say what each gap would cost the claim. That is the material a recommendation is built from; the checklist stops there.
