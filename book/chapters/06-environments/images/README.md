# Chapter 6 Figure Inventory

Active hand-authored figures:

- `F6-environment-tool-interface`: request checking, invocation, parsing, and
  run recording.
- `F6b-chipyard-framework`: one configuration feeding several authorized tool
  paths.
- `F5-artifact-lineage`: parent-child identity across software, hardware, and
  evaluated artifacts.

Deferred sources:

- `book/scratch_figures/deferred-chapter-06/F5-silicon-admet-funnel.svg`
  preserves the former drug-discovery analogy for the later visual pass. The
  streamlined chapter uses plain multi-tool language instead.
- `book/scratch_figures/deferred-chapter-06/simulator-tax.py` preserves the
  former executable slowdown chart. Its mixed instruction and cycle
  throughput anchors need a dedicated evidence pass before reuse.

The frozen source receipt remains at
`data/source-receipts/chapter5-simulator-tax.csv`. Its historical filename is
retained to avoid an unrelated rename.

The `.pdf` files beside active SVG sources are generated print companions.
